# 文档说明 — 这套流水线里的每份文档是什么

> 每份文档:谁写的、谁读、装什么。从零跑起来的步骤见 `启动说明.md`,
> 每一步具体干什么见 `工作流总览.md`。

按「谁写」分五类。**你只需要写第一类**;后面四类是跑出来的产物,看懂它们是为了知道
流水线在哪一步用了什么。

---

## 一、你写的(框架不替你编)

| 文档 | 大白话 | 不写的后果 |
|---|---|---|
| `idea.md`(项目根) | 你的创新点:一句话贡献、核心洞察、方法怎么设计的、跟前人差在哪 | 方法/引言/摘要这类章**拒绝开写**;`--expand` 也拒绝跑 |
| `outline.md`(项目根) | 整篇分几章、每章什么类型、每节写什么、每节多少字 | 跑不起来(`--init` 没输入) |
| `data/`(项目根) | 你的实验结果(CSV / JSON / 日志 / 图) | 结果/实验/消融这类章**拒绝开写**;`--init` 也不会生成 `data-index.md` |
| `references/bibliography.md` | 你的参考文献清单。`\cite{xxx}` 的 key 从这里来 | Stage 0 生成的 `input.md` 内容空洞;自动补引用全匹配不到 |

模板:`idea.example.md`、`outline.example.md`、`data/README.md`(告诉你 data/ 该放什么)。

> 注意:没有"各章 input.md"这种你手写的输入了。input.md 改由 Stage 0 时 Manager 从
> 你这份 `bibliography.md` 自动组织(见四)。`idea.md` 是全局一份、每个阶段 Agent 直接
> 读全文,不再复制进任何聚合文件。

---

## 二、Manager 写给你审的(中间唯一要你动手的)

| 文档 | 大白话 |
|---|---|
| `outline.expanded.md` | `--expand` 跑出来的:每章拆成哪几节、每节写什么要点。**你审完、补字数、改名成 `outline.md` 才算数** |

它不会被 `--init` 读——你这道审阅就是它存在的理由。没改名之前它一直是"待审"状态,下次
`--expand` 见到它会拒绝跑(怕覆盖你的审阅结果)。

---

## 三、Python / Manager 生成的(别手改)

| 文档 | 大白话 |
|---|---|
| 各章 `brief.md` | `outline.md` 在这一章的规格:类型 + 小节 + 字数,首行带 outline 指纹。Agent 写正文时读这一个 |
| `cross-chapter-state.md` | 所有章节共用的**"术语/符号备忘录"**。每章跑完自动把自己定的符号、一句话结论加进去;下一章直接抄用,不重复定义。删了 `--init` 会重建 |
| `data/data-index.md` | `--init` 时 Manager 读 `data/` 生成的**三级索引**(实验 → 结果 → 数值),data 类章节写正文时用它导航数字。存在则跳过不覆盖;数字门禁仍直读 `data/` 原文 |
| 各章 `cross-chapter-draft.md` | Stage 5 写的**草稿版**备忘录。先在草稿上写、检查没写错(没把前几章的内容冲掉)才正式替换;没通过会留着给你看问题在哪 |
| 各章 `number-check.md` | **数字对账单**:正文里的每个数能不能对回 `data/` 真实结果。两次门禁(Stage 1 后 + Stage 4 后)写同一个文件,后者覆盖前者,只留最终对账单;纯论述章不生成 |
| 各章 `citation-insertions.md` | **引用补全报告**:哪些句自动插了 `\cite`、哪些匹配不到留给你 |
| `latex/sections/*.tex` + `references.bib` | 编译 PDF 用的,从各章 `final.md` 和 bibliography 转出来 |

---

## 四、Agent 写的(按流水线顺序)

| 文档 | 写它的 Agent | 大白话 |
|---|---|---|
| `input.md`(各章) | Manager (Stage 0) | 本章该引的**文献素材**:相关 REF-ID + 每条对本章的意义。从 `bibliography.md` 挑选组织;不复述 idea、不列数字。已存在则跳过 |
| `evidence-pack.md` | Draft (Stage 0) | Agent 先当"审稿人":从 4 个角度对材料提硬问题再自答,答不出就写"答不出"。规划时读全文 |
| `draft-v1.plan.md` | Manager (Stage 1) | **施工图**:分几段、每段覆盖哪些小节、这一章的术语/符号统一定义成什么 |
| `draft-v1.part-N` → `draft-v1.md` | Draft (Stage 1) → Python 拼接 | 分几段起草,Python 拼成初稿 |
| `review-v1.md` + `review-v1.json` | Review (Stage 2) | `.md` 是**人能读的评审**(哪里好哪里差、怎么改);`.json` 是同一次评审的**机器版**,流水线代码读它——里面有打分、一份"**必须修清单**"(下一阶段照着逐条改)、一份"**这几句缺引用清单**"(照着插) |
| `draft-v2.round-N` + `review-verify.round-N` | Draft↔Review (Stage 3) | **收敛循环**:Draft 改、Review 验,改一轮验一轮,直到"必须修"清单清空或跑满 4 轮 |
| `final.md` / `final.zh.md` | Review (Stage 4) | 英文定稿 / 中文审阅版 |
| `decision.md` | Draft / Review | **取舍记录**:为什么这么写、放弃了什么 |
| `todo.md` | 每阶段 | 没解决的问题,交给下一阶段或交给你 |

---

## 五、写作规范(给 Agent 按需读,不是给你填的)

`skills/` 下五份写作和审稿规范。存成单独文件而不塞进提示词,是为了让 Agent 需要时才读、
不每次全量加载。谁什么时候读哪一份:

| 规范文件 | 谁按需读 | 什么时候读 |
|---|---|---|
| `writing-style.md` | Draft | 起草正文时(段落/句式/时态) |
| `figure-table-placeholder.md` | Draft | 有图表时(写占位符,不生成图——图你画) |
| `math-formula.md` | Draft | 有公式时(符号定义/LaTeX) |
| `review-rubric.md` | Review | 评审时(打分标准/防谄媚) |
| `experiment-writing.md` | Draft | 写实验论文时(数字纪律/防幻觉) |

> 图表占位符:Agent **不画图**,只写 `[FIGURE N: 标题] + 说明 + 数据来源 + 支撑论断`,
> 图由你画。表可以用结果库里的真实数字。

---

## type 词表(决定从哪取材)

`outline.md` / `brief.md` 里每章的 `type:` 决定这章从哪取材、数字管多严,以及**没标字数时的经验默认词数**:

| type | 从哪取材 | 数字怎么管 | 默认词数 | 中文也叫 |
|---|---|---|---:|---|
| `abstract` | idea.md | 有数就查、没数照写 | 200 | 摘要 |
| `intro` | idea.md | 有数就查、没数照写 | 700 | 引言 / 绪论 |
| `related` | idea.md + 文献 | 不查 | 700 | 相关工作 |
| `background` | idea.md + 文献 | 不查 | 700 | 背景 / 预备知识 |
| `method` | **idea.md** | 有数就查、没数照写 | 1500 | 方法 / 模型 |
| `theory` | idea.md | 有数就查、没数照写 | 1500 | 理论 |
| `experiments` | **data/** | 没 data/ **拒绝开写** | 1800 | 实验 / 实验设置 |
| `results` | **data/** | 没 data/ **拒绝开写** | 1800 | 结果 / 评估 |
| `analysis` | data/ | 没 data/ **拒绝开写** | 1200 | 分析 |
| `ablation` | data/ | 没 data/ **拒绝开写** | 1200 | 消融 |
| `discussion` | 两者 | 有数就查、没数照写 | 600 | 讨论 |
| `limitations` | 两者 | 有数就查、没数照写 | 200 | 局限 |
| `conclusion` | 两者 | 有数就查、没数照写 | 300 | 结论 |

> 「默认词数」是顶会经验值,省事用的默认,不是标准——没标 `(~N words)` 的小节按它取,
> 小节级 `- type:` 优先于章 type。投稿前按页数压缩,重要的章节手动标覆盖。

- 小节下写 `- type: results` 可以单独改这一节的取材源。框架按小节和按起草段分别取材。

---

## 项目自身的说明文档(不进流水线)

| 文档 | 内容 |
|---|---|
| `README.md` | 项目总览与快速开始 |
| `启动说明.md` | 从零到跑通的操作步骤 |
| `工作流总览.md` | 每一步读什么、产出什么、谁干 |
| `文档说明.md` | 本文档:每份文档是什么 |
| `TODO.md` | 待办与前置条件 |
| `data/README.md` | data/ 里该放什么(数据格式) |
