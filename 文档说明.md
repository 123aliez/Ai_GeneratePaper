# 文档说明 — 这套流水线里的每份文档是什么

> 每份文档:谁写的、谁读、装什么。从零跑起来的步骤见 `启动说明.md`,
> 每一步具体干什么见 `工作流总览.md`。

按「谁写」分五类。**你只需要写第一类**;后面四类是跑出来的产物,看懂它们是为了知道
流水线在哪一步用了什么。

---

## 一、你写的(框架不替你编)

| 文档 | 大白话 | 不写的后果 |
|---|---|---|
| `idea.md`(项目根) | 你的创新点:一句话贡献、核心洞察、方法怎么设计的、跟前人差在哪 | 方法/引言/摘要这类章**拒绝开写**;`--expand` 也拒绝跑 |
| `outline.md`(项目根) | 整篇分几章、每章什么类型、每节写什么、每节多少字 | 跑不起来(`--init` 没输入) |
| `data/`(项目根) | 你的实验结果(CSV / JSON / 日志 / 图) | 结果/实验/消融这类章**拒绝开写** |
| 各章 `input.md` | 这一章特有的补充素材(不属于全局 idea 的细节) | 可以空着,流水线照跑;**永远不被覆盖** |
| `references/bibliography.md` | 你的参考文献清单。`\cite{xxx}` 的 key 从这里来 | 自动补引用全匹配不到,全标给你手动处理 |

模板:`idea.example.md`、`outline.example.md`、`data/README.md`(告诉你 data/ 该放什么)。

---

## 二、Manager 写给你审的(中间唯一要你动手的)

| 文档 | 大白话 |
|---|---|
| `outline.expanded.md` | `--expand` 跑出来的:每章拆成哪几节、每节写什么要点。**你审完、补字数、改名成 `outline.md` 才算数** |

它不会被 `--init` 读——你这道审阅就是它存在的理由。没改名之前它一直是"待审"状态,下次
`--expand` 见到它会拒绝跑(怕覆盖你的审阅结果)。

---

## 三、Python 生成的(别手改)

| 文档 | 大白话 |
|---|---|
| 各章 `brief.md` | `outline.md` 在这一章的规格:类型 + 小节 + 字数。Agent 写正文时读这一个 |
| `cross-chapter-state.md` | 所有章节共用的**"术语/符号备忘录"**。每章跑完自动把自己定的符号、一句话结论加进去;下一章直接抄用,不重复定义。删了 `--init` 会重建 |
| 各章 `stage5-candidate.md` | Stage 5 写的**草稿版**备忘录。先在草稿上写、检查没写错(没把前几章的内容冲掉)才正式替换;没通过会留着给你看问题在哪 |
| 各章 `context-pack.md` | 这一章写正文时用的资料。把 `idea.md` 全文和 `data/` 结果按本章重点拼进一个文件:写方法章时 `idea.md` 在前、结果在后并标"只当背景";写结果章时反过来。Agent 写正文只读这一个文件 |
| 各章 `number-check.md` / `number-check-final.md` | **数字对账单**:正文里的每个数,能不能对回 `data/` 里的真实结果 |
| 各章 `citation-insertions.md` | **引用补全报告**:哪些句自动插了 `\cite`、哪些匹配不到留给你 |
| `latex/sections/*.tex` + `references.bib` | 编译 PDF 用的,从各章 `final.md` 和 bibliography 转出来 |

---

## 四、Agent 写的(按流水线顺序)

| 文档 | 写它的 Agent | 大白话 |
|---|---|---|
| `evidence-pack.md` | Draft (Stage 0) | Agent 先当"审稿人":从 4 个角度对材料提硬问题再自答,答不出就写"答不出"。规划时读全文 |
| `draft-v1.plan.md` | Manager (Stage 1) | **施工图**:分几段、每段覆盖哪些小节、这一章的术语/符号统一定义成什么 |
| `draft-v1.part-N` → `draft-v1.md` | Draft (Stage 1) → Python 拼接 | 分几段起草,Python 拼成初稿 |
| `review-v1.md` + `review-v1.json` | Review (Stage 2) | `.md` 是**人能读的评审**(哪里好哪里差、怎么改);`.json` 是同一次评审的**机器版**,流水线代码读它——里面有打分、一份"**必须修**清单"(下一阶段照着逐条改)、一份"**这几句缺引用**清单"(照着插) |
| `draft-v2.round-N` + `review-verify.round-N` | Draft↔Review (Stage 3) | **收敛循环**:Draft 改、Review 验,改一轮验一轮,直到"必须修"清单清空或跑满 4 轮 |
| `final.md` / `final.zh.md` | Review (Stage 4) | 英文定稿 / 中文审阅版 |
| `decision.md` | Draft / Review | **取舍记录**:为什么这么写、放弃了什么 |
| `todo.md` | 每阶段 | 没解决的问题,交给下一阶段或交给你 |

---

## 五、写作规范(给 Agent 按需读,不是给你填的)

`skills/` 下五份写作和审稿规范。存成单独文件而不塞进提示词,是为了让 Agent 需要时才读、
不每次全量加载。谁什么时候读哪一份:

| 规范文件 | 谁按需读 | 什么时候读 |
|---|---|---|
| `writing-style.md` | Draft | 起草正文时(段落/句式/时态) |
| `figure-table-placeholder.md` | Draft | 有图表时(写占位符,不生成图——图你画) |
| `math-formula.md` | Draft | 有公式时(符号定义/LaTeX) |
| `review-rubric.md` | Review | 评审时(打分标准/防谄媚) |
| `experiment-writing.md` | Draft | 写实验论文时(数字纪律/防幻觉) |

> 图表占位符:Agent **不画图**,只写 `[FIGURE N: 标题] + 说明 + 数据来源 + 支撑论断`,
> 图由你画。表可以用结果库里的真实数字。

---

## type 词表(决定从哪取材)

`outline.md` / `brief.md` 里每章的 `type:` 决定这章从哪取材、数字管多严:

| type | 从哪取材 | 数字怎么管 | 中文也叫 |
|---|---|---|---|
| `abstract` | idea.md | 有数就查、没数照写 | 摘要 |
| `intro` | idea.md | 有数就查、没数照写 | 引言 / 绪论 |
| `related` | idea.md + 文献 | 不查 | 相关工作 |
| `background` | idea.md + 文献 | 不查 | 背景 / 预备知识 |
| `method` | **idea.md** | 有数就查、没数照写 | 方法 / 模型 |
| `theory` | idea.md | 有数就查、没数照写 | 理论 |
| `experiments` | **data/** | 没 data/ **拒绝开写** | 实验 / 实验设置 |
| `results` | **data/** | 没 data/ **拒绝开写** | 结果 / 评估 |
| `analysis` | data/ | 没 data/ **拒绝开写** | 分析 |
| `ablation` | data/ | 没 data/ **拒绝开写** | 消融 |
| `discussion` | 两者 | 有数就查、没数照写 | 讨论 |
| `limitations` | 两者 | 有数就查、没数照写 | 局限 |
| `conclusion` | 两者 | 有数就查、没数照写 | 结论 |

- 小节下写 `- type: results` 可以单独改这一节的取材源。框架按小节和按起草段分别取材。

---

## 项目自身的说明文档(不进流水线)

| 文档 | 内容 |
|---|---|
| `README.md` | 项目总览与快速开始 |
| `启动说明.md` | 从零到跑通的操作步骤 |
| `工作流总览.md` | 每一步读什么、产出什么、谁干 |
| `文档说明.md` | 本文档:每份文档是什么 |
| `TODO.md` | 待办与前置条件 |
| `data/README.md` | data/ 里该放什么(数据格式) |
