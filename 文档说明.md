# Ai_GeneratePaper — 文件清单与交付状态

实验数据驱动的多 Agent 论文写作框架。从 `survey/` 的综述引擎复制并改造而来:
内容源从"文献笔记库"换成"**创新点文档 + 实验结果库**",并补齐了检索、门禁、收敛、
防幻觉机制,以及 outline 驱动的逐章生成。

**三份人工输入,职责严格分开**:

| 文件 | 管什么 | 缺了会怎样 |
|---|---|---|
| `idea.md` | 贡献是什么 | idea 类章节**拒绝起草**(贡献只有你能给) |
| `outline.md` | 结构是什么 | 只能用逐章模式(手建文件夹 + 手写 brief) |
| `data/` | 数字是什么 | data 类章节**拒绝起草** |

**两种写作模式,两条独立路由**:整篇模式(`--init` 生成各章文件夹)与逐章模式
(你自己建文件夹)。判据只有一条:该文件夹在不在 `outline.md` 里。两条路由对 Agent 的
要求相反,指令分开下——详见 `工作流总览.md`。

**状态图例**

| 标记 | 含义 |
|---|---|
| ✅ 已建并测 | 代码写好,通过 import + 自测(离线,不调 API) |
| 🔶 已建待接线 | 模块可用、可单测,但尚未接进主流水线 |
| 🟡 部分实现 | 机制到位但非完全体(下方注明差在哪) |
| 📥 需你提供 | 框架留了位,内容要你填 |
| ⏳ 需真实数据/密钥才能验 | 代码就绪,端到端跑通依赖你的输入 |

---

## 一、核心引擎

| 文件 | 状态 | 说明 |
|---|---|---|
| `config.py` | ✅ | 配置。`PAPER_MODE`(experiment/survey)、`IDEA_PATH`、`DATA_ROOT`、`RETRIEVAL_*`、`MAX_REVISION_ROUNDS` |
| `run.py` | ✅ | CLI 入口。`--init [--force]` / `--all` / 单章 / `--list`,每条命令都会先打印路由 |
| `agents/agents.py` | ✅ | 三 Agent 装配(Draft / Review / Manager),五个共享工具 |
| `agents/prompts.py` | ✅ | 三份系统指令。跨章上下文改为**按写作模式条件读取**(见下方"已修正的架构偏差") |
| `agents/orchestrator.py` | 🟡 | 六阶段流水线 + 收敛循环 + 双路由接线 + 路由指纹 + Stage 5 校验 |
| `agents/tools.py` | ✅ | `read_file` / `write_file` / `list_folder` / `search_references` / `search_literature` |
| `generate_reference_index.py` | ✅ | 文献索引生成器(survey 沿用) |

---

## 二、机制模块

| 文件 | 状态 | 职责 |
|---|---|---|
| `agents/outline.py` | ✅ | **outline 解析 + 双写作路由**。`parse_outline` / `init_chapter_workspaces` / `resolve_write_mode` / `build_mode_clause` / `build_mode_review_clause` / `refresh_cross_chapter_state`。含 outline 指纹(sha256,跨进程稳定)与文件夹碰撞检测 |
| `agents/chapter_type.py` | ✅ | **章节类型路由**:`type:` → (取材源 family, 门禁级别 gate)。13 种类型 + 中英别名 + 小节级覆盖 + 文件夹名兜底 |
| `agents/content_source.py` | ✅ | 内容源抽象:按 family 排序证据(idea 章 `idea.md` 优先、data 章结果表优先);`idea.md` 缺失**或未填**时明确声明而非编造 |
| `agents/evidence_mining.py` | ✅ | 起草前多视角提问(STORM)。**视角随类型切换**:idea 章问机制/新颖性,data 章问显著性/baseline |
| `agents/number_gate.py` | ✅ | 数字一致性门禁:正文数字比对结果库,邻近度绑定 + 量纲缩放容错 |
| `agents/citation_check.py` | ✅ | 引用闭合校验 + LaTeX 日志解析 |
| `agents/citation_supplement.py` | ✅ | 缺引用自动补,带 URL 验证 + "禁编造"降级(选不到就标 needs_human) |
| `agents/retrieval.py` | ✅ | 两层检索:本地 bib(tier-1,可引)+ 网页 LLM(tier-2,**仅线索,不可引**)。已接成 `search_literature` 工具,三个 Agent 都能调 |

---

## 三、测试(全部离线,不调 API、不花 token)

| 文件 | 检查数 | 覆盖 |
|---|---|---|
| `tests/test_routing.py` | 50 | 类型路由**单元**:type 解析/别名/兜底/小节联合/分段路由/视角切换/审稿判据/context pack 排序/缺 idea 声明 |
| `tests/test_outline.py` | 141 | **outline 与双路由**:解析(含代码块跳过、非章节 `##` 过滤、自动编号)、brief 往返(渲染 → `parse_brief_sections` → `resolve_run_route`)、outline 指纹稳定性、`--init` 不覆盖手改、FULL/SINGLE 判定、两套 mode clause 内容相反、跨章状态生成与 `--force` 保留累积内容 |
| `tests/test_pipeline_routing.py` | 100 | **流水线级**:用记录型假 Agent 驱动完整 `run_4stage_with_progress`。pre-flight 门禁真的在调模型前拦住、分段路由正确、两条写作路由的契约到达**每一个**改写正文的阶段、Stage 5 只在 FULL 下跑且校验到位、路由变化硬停且不删文件 |
| `tests/test_optimizations.py` | 61 | 分段自适应、survey 路径守卫、路由指纹(含 write_mode)、`search_literature` 接线 |

```bash
# 改完 outline/brief/idea 后先跑这四套,确认接线正常
.venv/bin/python tests/test_routing.py
.venv/bin/python tests/test_outline.py
.venv/bin/python tests/test_pipeline_routing.py
.venv/bin/python tests/test_optimizations.py
```

---

## 四、编译链与脚手架

| 文件 | 状态 | 说明 |
|---|---|---|
| `latex/build.py` | ✅ | 编译前跑引用闭合门禁 → 编译 → 带日志再解析 undefined。门禁不过非零退出 |
| `latex/main.tex` | 📥 | LaTeX 模板骨架 |
| `README.md` | ✅ | 项目总览 + 快速开始 |
| `工作流总览.md` | ✅ | **多 Agent 工作流全文**:每阶段读什么、产出什么、谁执行 |
| `文档说明.md` | ✅ | 本文件 |
| `启动说明.md` | ✅ | 逐步启动指引 |
| `TODO.md` | ✅ | 已知差距与待办 |
| `.env.example` | ✅ | 环境变量模板(三组模型 key + 可选检索 LLM) |
| `requirements.txt` | ✅ | 依赖(smolagents + dotenv;**未含 torch**) |
| `data/README.md` | ✅ | **数据契约**——定义你要放什么进 `data/` |
| `data/final_info.example.json` / `results.example.csv` | ✅ | 结果文件示例 |
| `idea.example.md` | ✅ | **创新点文档模板**:一句话贡献 / 问题动机 / 核心洞察 / 方法设计(含符号)/ 与前人 delta / 贡献清单 / 已知局限 / 预期结论 |
| `outline.example.md` | ✅ | **论文总纲模板**:两级标题 + 类型表 + 格式说明 |
| `references/bibliography.md` | 📥 | 参考文献表模板 |
| `workspace/_TEMPLATE/brief.md` | 📥 | 章节规格模板(含 `type:` 声明表)。**逐章模式**手建文件夹时照它写;整篇模式由 `--init` 自动生成,不需要它 |

---

## 五、需要你提供的东西

| # | 你要提供 | 放哪 | 为什么 |
|---|---|---|---|
| 1 | **创新点文档** ⭐ | `cp idea.example.md idea.md`(项目根) | 📥 **最重要的一份**。Method/Intro/Related/Abstract 全靠它。缺失**或还是未填的模板**→ idea 类章节直接拒绝起草 |
| 2 | **论文总纲** ⭐ | `cp outline.example.md outline.md`(项目根) | 📥 整篇模式的结构来源。写两级标题:`##` 是章(带 `type:`),`###` 是小节(带 `(~N words)`)。只写单章可以不要它 |
| 3 | **实验结果** | `data/` 下(CSV/JSON/log/图) | 📥⏳ Results/Experiments/Ablation 的内容源,格式见 `data/README.md`。缺它只拦 data 类章节 |
| 4 | **API 密钥**(Draft/Review/Manager 三组 + 可选检索 LLM) | `cp .env.example .env` 填入 | ⏳ 不填无法调模型 |
| 5 | **各章素材** | `workspace/<章>/input.md` | 📥 `--init` 会按小节生成骨架,你往里填。**永不被覆盖**,`--force` 也不动它 |
| 6 | **参考文献** | `references/bibliography.md` | 📥 引用闭合校验和自动补引用要对账它 |
| 7 |(可选)**LaTeX 模板调整** | `latex/main.tex` | 📥 想编译 PDF 时 |

`idea.md`、`outline.md`、`data/` 的内容都被 `.gitignore` 挡掉(未发表的东西不进版本库),
模板和契约文档进。

### `outline.md` 怎么写

```markdown
## 4. Method

type: method

### 4.1 总体框架 (~250 words)
- 从 idea.md 的「方法设计」展开
- 先给全局数据流,再拆模块

### 4.2 谱域重标定模块 (~350 words)
- 公式与符号定义
- 为什么频域有效(机制,不是结果)
```

- `##` 是章,`###` 是章内小节。章号可省(按出现顺序自动编号)
- 每章一行 `type:`(合法值见下表)。不写会从标题推断
- 小节的 `(~N words)` 可省,默认 250。小节下的 `-` 要点会写进 brief 给 Agent 看
- 小节也能单独写 `- type: results` 覆盖该节的取材源
- 代码块(``` 围起来的)里的 `##` 不会被当成章节声明
- outline 顶部的「写法」「说明」这类 `##` 小标题会被自动过滤(既没 `type:`、
  标题也推不出类型、且没有 `###` 小节的,不算章)

### `type:` 写什么

| 写这个 | 主输入 | 数字门禁 | 中文别名 |
|---|---|---|---|
| `abstract` | idea.md | advisory | 摘要 |
| `intro` | idea.md | advisory | 引言 / 绪论 |
| `related` | idea.md + 文献 | off | 相关工作 |
| `background` | idea.md + 文献 | off | 背景 / 预备知识 |
| `method` | **idea.md** | advisory | 方法 / 模型 |
| `theory` | idea.md | advisory | 理论 |
| `experiments` | **data/** | blocking | 实验 / 实验设置 |
| `results` | **data/** | blocking | 结果 / 评估 |
| `analysis` | data/ | blocking | 分析 |
| `ablation` | data/ | blocking | 消融 / 消融实验 |
| `discussion` | 两者 | advisory | 讨论 |
| `limitations` | 两者 | advisory | 局限 / 不足 |
| `conclusion` | 两者 | advisory | 结论 / 总结 |

- 写错的类型会退回 mixed/advisory 并在终端提示合法值,不会静默
- 不写会尝试从标题或文件夹名推断(`05-results` → results),仍推不出就 mixed/advisory 并提示补

---

## 六、已修正的架构偏差(诚实说明)

### 每章都读结果库、每章都过 fail-closed 数字门禁

早期版本如此。两个后果:方法章被写成结果复述(创新点这个真正的主输入根本没进上下文),
而且实验没跑完就没法先写方法。现已按类型分流,六处联动:

| 环节 | 修正 |
|---|---|
| context pack | idea 章把 `idea.md` 排最前标 PRIMARY、结果表降级成 SUPPORTING;data 章反过来 |
| 证据挖掘视角 | idea 章问「新颖性/机制/精确性/替代设计」,data 章问「显著性/baseline/可复现/过度声称」 |
| 起草提示词 | 按**每个起草段自己覆盖的小节**路由,不是整章一刀切 |
| 审稿判据 | idea 章明确告诉审稿"不要索要统计显著性、缺指标不算缺陷";data 章要求逐个数字对账 |
| 数字门禁 | blocking(data 章,无数据拒绝起草)/ advisory(idea 章,无数据只提示 UNVERIFIED)/ off(纯论述章不跑) |
| pre-flight | **两个主输入都在调模型前检查**:idea 类缺 `idea.md`、data 类缺 `data/` → 直接中止。原先 data 类要等 Stage 0+1 跑完(四次模型调用)才被拦住 |

### 两种写作模式共用一份指令

后果是**双向**的:整篇模式下每章都自我重复定义一遍符号;逐章模式下每章都写着
"承接上一章"而上一章并不存在。现已分成两条路由,五处联动:

| 环节 | 修正 |
|---|---|
| 路由判定 | 判据是"该文件夹在不在 outline.md 里",不看命令行参数 |
| 起草契约 | FULL:第几章/可跨章引用/复用前章符号/接上文引下文。SINGLE:自包含/禁跨章过渡句 |
| 审稿判据 | FULL:查重复定义、查跨章重复、查开合缺失。SINGLE:未定义符号与跨章引用算 MUST FIX |
| 覆盖范围 | 契约注入**每一个**改写正文的阶段:1a 规划、1b~1c 各段、Stage 3 每一轮、Stage 4 定稿 |
| Stage 5 | 只在 FULL 下跑。SINGLE 章的术语不写进跨章状态(否则会污染整篇的约定) |

### Agent 系统指令无条件要求读跨章上下文

这是上一条的隐藏一半。三份系统指令原本写着"读 `cross-chapter-state.md`、读
`paper/01 Structure/final.md`、读前一章的 `final.md`"。系统指令**优先级高于** task prompt,
所以只在 task prompt 里不提跨章状态是无效的——逐章模式下 Agent 照样会去翻。
现已改成按 WRITING MODE 条件读取,并在 SINGLE 契约里显式禁止。

### 跨章状态"记得手动更新"

原先 Stage 4 之后只打印一行提示。漏一次,后面章节的术语就开始漂。现在是自动的 Stage 5,
带幂等契约(`- [<章名>] ` 前缀 upsert)和 Python 后置校验(三个标题结构完好 +
Key Claims 小节内有本章标记)。校验失败 → 本章判为未完成,`--all` 停下。

### 断点续跑静默复用旧路由的产物

每个产物"存在即跳过",所以改了路由直接重跑,会拿着旧路由的 evidence-pack / plan /
review 继续写,而流水线全程显示成功。现在把路由(证据路由 + 写作模式)写进
`context-pack.md` 首行指纹,不一致就列出陈旧产物并**硬停**——一个文件都不删,
但也绝不继续(继续会覆盖指纹,警告下一次就不再出现)。

### 其他已修

- **段数硬编 3**:Abstract 这类只有 1-2 节的章会生出没有来源的空段,并给它们 700 词的目标。
  现在段数 `min(小节数, 3)`,拼接时也按本次实际段数拼(否则上次跑三段留下的 part-3
  会被拼进这次的两段草稿)。
- **Alignment 章硬编特例**:从 survey 引擎继承来的按标题特判,对实验论文没有意义,已删。
- **outline 指纹用内置 `hash()`**:受 `PYTHONHASHSEED` 随机化影响,`--init` 与实际运行是两次
  进程,指纹永远对不上。已改用 sha256。
- **codex 审查的 Critical 全修**:C1(实验模式强制 `--progress`)、C2(收敛严格验收,
  空清单不假通过)、C3(无效 review JSON 强制重跑)、C5(证据挖掘被 planner 消费)、
  C6(实验模式不注入 survey 索引)、C7(数字门禁 fail-closed)、C11(引用补全落地成
  `\cite` 插入)。

---

## 七、已知差距

**🟡 增量补丁修改(Stage 3 收敛循环)**
现状:让 Draft "只改问题处、保留正确段落",是**提示词层面的引导**,不是真正的
SEARCH/REPLACE 补丁应用。真·补丁(Python 定位段落做无损替换、匹配失败兜底)还没做。
影响:省 token 和"护住好段落"的效果不如确定性版本。要不要升级,你定。

**🟡 章节字数标准**
`format_stage1_parts` 目前按比例给区间(`target*0.85` ~ `target*1.2+30`)。
按顶会各章节平均字数写一份参考进提示词的事项**待办**。

**🟡 剩余 Important / Minor**
codex 审查发现的 Important 修了核心几个,剩余多数是健壮性打磨(防 SSRF、真 web 检索等),
在"web 检索默认关"的前提下触发概率低,等真数据试跑暴露问题再针对性修。

**⏳ 端到端未跑(等你数据)**
所有验证都是**离线的**:import 通过、确定性门禁(引用闭合/数字一致/自动补引/检索)自测通过、
四套测试共 352 项检查通过、pre-flight 与路由门禁已验证不调模型、CLI 各路径正常。
但**没用真 API + 真数据跑过一整篇**。真实 LLM 能否稳定吐合法 JSON、VERIFY 判定准不准、
证据挖掘质量如何——要等你拿数据 + 密钥试跑才知道。

---

## 八、启动步骤

### 整篇模式(写一整篇)

```bash
pip install -r requirements.txt
cp .env.example .env                      # 填三组模型 key(+ 可选检索 LLM)

cp idea.example.md idea.md                # ⭐ 写贡献/原理/方法设计(最重要)
cp outline.example.md outline.md          # ⭐ 写整篇章节结构
# 把实验结果放进 data/(格式见 data/README.md)
# 把 references/bibliography.md 填上文献

python run.py --init                      # outline.md → 各章工作区 + 跨章状态
# 填各章的 workspace/<章>/input.md

python run.py --list                      # 确认每章的路由与类型都对
python run.py --all --progress            # 按 outline 顺序跑全部
python latex/build.py                     # 编译 + 引用门禁
```

### 逐章模式(只写单独一章)

```bash
mkdir -p workspace/my-chapter
cp workspace/_TEMPLATE/brief.md workspace/my-chapter/brief.md
# 编辑 brief.md:顶部写 type:,再定义小节
# 写 workspace/my-chapter/input.md 放素材

python run.py my-chapter --progress
```

**开跑前先看两行终端输出**:

- `[Manager] route |` — 解析出的类型、取材源、门禁级别和各小节路由
- `[Manager] write-mode |` — 整篇第几章 / 自包含单章

和你的预期不符,改 `outline.md` 或 `brief.md`,不改代码。
