## 一、你写的(框架不替你编)

| 文档                           | 大白话                              | 不写的后果                                              |
| ---------------------------- | -------------------------------- | -------------------------------------------------- |
| `idea.md`(项目根)               | 你的创新点:一句话贡献、核心洞察、方法怎么设计的、跟前人差在哪  | 方法/引言/摘要这类章**拒绝开写**;`--expand` 也拒绝跑                |
| `outline_draft.md`(项目根)         | 你填的章节骨架:分几章、每章什么类型、什么顺序         | 跑不起来(`--init` 没输入)                                 |
| `data/`(项目根)                 | 你的实验结果(CSV / JSON / 日志 / 图)      | 结果/实验/消融这类章**拒绝开写**;`--init` 也不会生成 `data-index.md` |
| `references/bibliography.md` | 你的参考文献清单。`\cite{xxx}` 的 key 从这里来 | Stage 0 生成的 `input.md` 内容空洞;自动补引用全匹配不到             |

模板:`idea.example.md`、`outline.example.md`、`data/README.md`(告诉你 data/ 该放什么)。

## 二、规划者写给你审的(中间唯一要你动手的)

| 文档 | 大白话 |
|---|---|
| `outline.zh.md`(项目根) | `--expand` 跑出来的**中文版**:每章拆成哪几节、每节写什么要点。**你审完、补字数、改这份** |
| `outline.md`(项目根) | `--expand` 跑出来的**英文版**:喂给 Agent 的结构。你改完中文版后,`--init` 会自动把中文翻译成英文**覆盖**它 |

**你只改中文版 `outline.zh.md`。** 英文版 `outline.md` 是给 Agent 吃的,`--init` 时若
检测到中文版更新会自动翻译覆盖——不用你手动同步。`outline.md` 不进 git(`.gitignore` 挡掉)。

---

## 三、Python / 规划者 生成的(别手改)

| 文档                                        | 大白话                                                                                             |
| ----------------------------------------- | ----------------------------------------------------------------------------------------------- |
| 各章 `brief.md`                             | `outline.md` 在这一章的规格:类型 + 小节 + 字数,首行带 outline 指纹。Agent 写正文时读这一个                                 |
| `cross-chapter-state.md`                  | 每章跑完自动把自己定的符号、一句话结论加进去;下一章直接抄用,不重复定义。删了 `--init` 会重建                                            |
| `data/data-index.md`                      | `--init` 时规划者读 `data/` 生成的**三级索引**(实验 → 结果 → 数值),data 类章节写正文时用它导航数字。存在则跳过不覆盖;数字门禁仍直读 `data/` 原文 |
| 各章 `cross-chapter-draft.md`               | Stage 5 写的**草稿版**备忘录。先在草稿上写、检查没写错(没把前几章的内容冲掉)才正式替换;没通过会留着给你看问题在哪                                |
| 各章 `number-check.md`                      | **数字对账单**:正文里的每个数能不能对回 `data/` 真实结果。两次门禁(Stage 1 后 + Stage 4 后)写同一个文件,后者覆盖前者,只留最终对账单;纯论述章不生成    |
| 各章 `citation-insertions.md`               | **引用补全报告**:哪些句自动插了 `\cite`、哪些匹配不到留给你                                                            |
| `latex/sections/*.tex` + `references.bib` | 编译 PDF 用的,从各章 `final.md` 和 bibliography 转出来                                                     |

---

## 四、Agent 写的(按流水线顺序)

| 文档 | 写它的 Agent | 大白话 |
|---|---|---|
| `input.md`(各章) | 规划者(Stage 0a) | 本章该引的**文献素材**:相关 REF-ID + 每条对本章的意义。从 `bibliography.md` 挑选组织;不复述 idea、不列数字。已存在则跳过 |
| `evidence-pack.md` | 起草者(Stage 0b) | Agent 先当"审稿人":从 4 个角度对材料提硬问题再自答,答不出就写"答不出"。规划时读全文 |
| `draft-v1.plan.md` | 规划者(Stage 1a) | **施工图**:分几段、每段覆盖哪些小节、这一章的术语/符号统一定义成什么 |
| `draft-v1.part-N` → `draft-v1.md` | 起草者(Stage 1b)→ Python 拼接 | 分几段起草,Python 拼成初稿 |
| `review-v1.md` + `review-v1.json` | 评审者(Stage 2) | `.md` 是**人能读的评审**(哪里好哪里差、怎么改);`.json` 是同一次评审的**机器版**,流水线代码读它——里面有打分、一份"**必须修清单**"(下一阶段照着逐条改)、一份"**这几句缺引用清单**"(照着插) |
| `draft-v2.round-N` + `review-verify.round-N` | 起草者↔评审者(Stage 3) | **收敛循环**:起草者改、评审者验,改一轮验一轮,直到"必须修"清单清空或跑满 4 轮 |
| `final.md` / `final.zh.md` | 评审者(Stage 4) | 英文定稿 / 中文审阅版 |
| `decision.md` | 起草者 / 评审者 | **取舍记录**:为什么这么写、放弃了什么 |
| `todo.md` | 每阶段 | 没解决的问题,交给下一阶段或交给你 |

---

## 五、写作规范(给 Agent 按需读,不是给你填的)

`skills/` 下五份写作和审稿规范。存成单独文件而不塞进提示词,是为了让 Agent 需要时才读、
不每次全量加载。谁什么时候读哪一份:

| 规范文件 | 谁按需读 | 什么时候读 |
|---|---|---|
| `writing-style.md` | 起草者 | 起草正文时(段落/句式/时态) |
| `figure-table-placeholder.md` | 起草者 | 有图表时(写占位符,不生成图——图你画) |
| `math-formula.md` | 起草者 | 有公式时(符号定义/LaTeX) |
| `review-rubric.md` | 评审者 | 评审时(打分标准/防谄媚) |
| `experiment-writing.md` | 起草者 | 写实验论文时(数字纪律/防幻觉) |

> 图表占位符:Agent **不画图**,只写 `[FIGURE N: 标题] + 说明 + 数据来源 + 支撑论断`,
> 图由你画。表可以用结果库里的真实数字。

---

## type 词表(决定从哪取材)

`outline.md` / `brief.md` 里每章的 `type:` 决定这章从哪取材、数字管多严,以及**没标字数时的经验默认词数**:

| type | 从哪取材 | 数字怎么管 | 默认词数 | 中文也叫 |
|---|---|---|---:|---|
| `abstract` | idea.md | 有数就查、没数照写 | 200 | 摘要 |
| `intro` | idea.md | 有数就查、没数照写 | 700 | 引言 / 绪论 |
| `related` | idea.md + 文献 | 不查 | 700 | 相关工作 |
| `background` | idea.md + 文献 | 不查 | 700 | 背景 / 预备知识 |
| `method` | **idea.md** | 有数就查、没数照写 | 1500 | 方法 / 模型 |
| `theory` | idea.md | 有数就查、没数照写 | 1500 | 理论 |
| `experiments` | **data/** | 没 data/ **拒绝开写** | 1800 | 实验 / 实验设置 |
| `results` | **data/** | 没 data/ **拒绝开写** | 1800 | 结果 / 评估 |
| `analysis` | data/ | 没 data/ **拒绝开写** | 1200 | 分析 |
| `ablation` | data/ | 没 data/ **拒绝开写** | 1200 | 消融 |
| `discussion` | 两者 | 有数就查、没数照写 | 600 | 讨论 |
| `limitations` | 两者 | 有数就查、没数照写 | 200 | 局限 |
| `conclusion` | 两者 | 有数就查、没数照写 | 300 | 结论 |

> 「默认词数」是顶会经验值,省事用的默认,不是标准——没标 `(~N words)` 的小节按它取,
> 小节级 `- type:` 优先于章 type。投稿前按页数压缩,重要的章节手动标覆盖。

- 小节下写 `- type: results` 可以单独改这一节的取材源。框架按小节和按起草段分别取材。

