# Idea — <你的方法名>

> **这是全篇论文最重要的一份输入文档,由你来写。**
>
> 修改本文件为 `idea.md`(项目根目录),框架会自动读取。

> 论文的贡献是**创新点和原理**
> Method / Introduction / Related Work 这些章节以本文档为主输入;只有
> Experiments / Results / Ablation 章才以 `data/` 结果库为主输入。
>
> 框架**绝不替你编造创新点**。本文档没写到的设计细节,起草时会标成
> `[DESIGN DETAIL NEEDED]` 交回给你,而不是"合理地"补一个。

---

## 1. 一句话贡献

<把整篇论文的贡献压缩成一句话。这句会被 Abstract 和 Introduction 反复用到。>

例:我们提出 Spec 模块——一种即插即用的谱域重标定单元,在几乎不增加参数量的前提下
提升卷积网络的细粒度分类精度。

## 2. 问题与动机

- **现状哪里不行**:<现有方法的具体缺陷,越具体越好>
- **为什么这个缺陷重要**:<不解决会怎样>
- **为什么之前没被解决**:<难点在哪、别人卡在哪>

## 3. 核心洞察

<为什么你的方法能work?这是审稿人最想看到的一段,也是最不能靠数据倒推出来的一段。>

## 4. 方法设计

写清楚**可复现级别**的细节。Method 章直接从这节展开。

### 4.1 总体结构
<输入是什么、经过哪些模块、输出是什么。可以用文字描述数据流。>

### 4.2 关键组件
| 组件 | 输入 | 输出 | 作用 |
|---|---|---|---|
| <组件A> | <> | <> | <> |

### 4.3 公式与符号
<关键公式用 LaTeX 写。每个符号给出定义——这些会进入全章 Notation Table,
所有章节统一沿用。>

例:
- $\mathbf{x} \in \mathbb{R}^{C \times H \times W}$:输入特征图,$C$ 为通道数
- $g(\cdot)$:谱域重标定函数,定义为 $g(\mathbf{x}) = \sigma(\mathbf{W}_2\,\delta(\mathbf{W}_1 \mathcal{F}(\mathbf{x})))$

### 4.4 设计选择与替代方案
<每个关键选择,说明"为什么这样"而不是"还可以那样"。审稿人一定会问。>

| 选择 | 我们的做法 | 替代方案 | 为什么选我们的 |
|---|---|---|---|
| <> | <> | <> | <> |

## 5. 与最近工作的区别

对每个最相关的工作,说清楚**差在哪**。Related Work 章从这节展开,
引用请用 `references/bibliography.md` 里的 REF-ID。

| 相关工作 | REF-ID | 它做了什么 | 我们的不同 |
|---|---|---|---|
| <> | REF-000X | <> | <> |

## 6. 论文声称的贡献清单

按重要性排序,一条一句。这份清单是"允许声称的上限"——框架不会替你加,
审稿 Agent 也会检查正文有没有声称超出这份清单的东西。

1. <贡献1>
2. <贡献2>
3. <贡献3>

## 7. 已知局限

<你自己知道的短板。主动写出来,Limitations / Discussion 章会用到;
不写的话审稿 Agent 也会挖,但不如你自己说得准。>

## 8. 预期结论

<读者读完应该记住什么。Conclusion 章从这节收口。注意:这里写的是
"论点",不是"数字"——具体数字由 data/ 结果库提供,框架会做数字比对门禁。>
