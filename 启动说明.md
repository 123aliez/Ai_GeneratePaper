# 启动说明(你自己运行)

实验数据驱动的多 Agent 论文写作框架。这份文档告诉你**从零跑起来要做什么**,以及**每一步的命令**。

要点先说:框架要三份你写的文件——`idea.md`(贡献是什么)、`outline.md`(结构是什么)、
`data/`(数字是什么)。章节文件夹全部由 `outline.md` 经 `--init` 生成。

---

## 一、一次性准备(首次运行前)

### 1. 装依赖
```bash
cd D:\vault\ai_vault\paper_agent
pip install -r requirements.txt
```

### 2. 配置 API key
```bash
copy .env.example .env    # Windows
# 然后编辑 .env,填每个 Agent 的 model / key / base
```
三个 Agent 独立配置,模型和 key 可以不同:
- `DRAFT_*` — 起草 Agent(可单独用一个模型/key)
- `REVIEW_*` — 评审 Agent
- `MANAGER_*` — 规划/编排 Agent

**模型名/接口匹配规则**(踩过的坑):
- OpenAI 兼容接口:模型名带 `openai/` 前缀,base **带 `/v1`**,如 `openai/gpt-5.4` + `https://xxx/v1`
- Anthropic 接口:模型名带 `anthropic/` 前缀,base **不带 `/v1`**(litellm 自动拼 `/v1/messages`)
- 同一个中转站建议统一前缀,别混(混了会报 INVALID_API_KEY / 404)

### 3. 验证配置
```bash
python -c "from config import get_draft_model; print(get_draft_model().model_id)"
```
能打印模型名、不报错,说明配置读到了。

### 4. 写创新点文档 ⭐(最重要的一步)
```bash
copy idea.example.md idea.md    # Windows
```
编辑 `idea.md`,填这 8 节:一句话贡献 / 问题动机 / **核心洞察** / **方法设计**(含公式符号) /
与前人的 delta / 贡献清单 / 已知局限 / 预期结论。

**为什么它比 `data/` 重要**:论文的贡献是创新点和原理,实验数据只是支撑。Method / Intro /
Related / Abstract 章全从这份文档取材。框架**绝不替你编造创新点或机制**——这份文档没写到的
设计细节,起草时会标成 `[DESIGN DETAIL NEEDED]` 交回给你。写得越具体,起草质量越高。

没有 `idea.md` 时,idea 类章节会在**调模型之前**直接拒绝起草并提示你。

### 5. 放实验数据
把实验结果放进 `data/`(格式见 `data/README.md` 和 `data/results/README.md`):
```
data/results/
├── run_0_baseline/final_info.json
├── run_1_ours/final_info.json
└── run_2_ablation/final_info.json
```
每个 run 一个文件夹,**文件夹名就是 run 标识**(会成为对比表行名)。`final_info.json` 里 `means` 是必填数字。

数据没跑完也能先写方法章:`type: method` 的门禁是 advisory,`data/` 为空照样起草。
只有 `type: results` 这类章节会在无数据时被拦。

### 6. 填参考文献
编辑 `references/bibliography.md`,加入你的文献(`| REF-0001 | ... |` 格式)。引用补全会从这里匹配 key。

### 7. 写论文结构 ⭐(整篇模式必需)
```bash
copy outline.example.md outline.md    # Windows
```
编辑 `outline.md`。`##` 是章、`###` 是章内小节:

```markdown
## 4. Method

type: method

### 4.1 总体框架 (~250 words)
- 从 idea.md 的「方法设计」展开
- 先给全局数据流,再拆模块

### 4.2 谱域重标定模块 (~350 words)
- 公式与符号定义
- 为什么频域有效(机制,不是结果)
```

每章一行 `type:`,它决定这章从哪取材、门禁多严:

| 写这个 | 主输入 | 数字门禁 | 中文别名也认 |
|---|---|---|---|
| `abstract` / `intro` | idea.md | advisory | 摘要 / 引言 |
| `related` / `background` | idea.md + 文献 | off | 相关工作 / 背景 |
| `method` / `theory` | **idea.md** | advisory | 方法 / 理论 |
| `experiments` / `results` | **data/** | blocking | 实验 / 结果 |
| `analysis` / `ablation` | data/ | blocking | 分析 / 消融 |
| `discussion` / `limitations` / `conclusion` | 两者 | advisory | 讨论 / 局限 / 结论 |

小节也能写 `- type: results` 覆盖该节的取材源——框架按小节和按起草段分别路由,
Abstract 段读 `idea.md`、Results 段读 `data/`,互不干扰。

### 8. 生成各章工作区
```bash
python run.py --init
```
它会为每章建 `workspace/<NN-章名>/`,里面放好 `brief.md`(本章规格,由 outline 生成),
外加 `workspace/cross-chapter-state.md`(跨章状态)。若 `data/` 里有结果,还会顺带让
规划者生成 `data/data-index.md`(data 类章节写数字时的导航)。

`input.md` **不由你手填,也不由 `--init` 生成**:跑某章的 Stage 0a 时规划者会从你的
`references/bibliography.md` 自动组织它(已存在则跳过)。所以 `--init` 之后可以直接跑章节。

> 改了 `outline.md` 之后要跑 `python run.py --init --force` 刷新 brief。
> 忘了刷新的话,运行时会**硬停**并提示你——不会拿旧规格继续跑。

### 9. 自检(离线,不花钱)
```bash
python tests/test_routing.py           # 类型解析、别名、分段路由、判据切换
python tests/test_outline.py           # outline 解析、brief 往返、写作契约
python tests/test_pipeline_routing.py  # 假 Agent 驱动完整流水线,验证门禁接线
python tests/test_optimizations.py     # 分段自适应、指纹、工具链
python tests/test_expand.py            # --expand 的越权改结构校验
```

---

## 二、日常运行

```bash
cd D:\vault\ai_vault\paper_agent

python run.py --list                  # 每章的类型 + 状态
python run.py --all --progress        # 按 outline 顺序跑全部,失败即停
python run.py "04-method" --progress  # 也可以只跑其中一章(必须在 outline 里)
```

每章知道自己是第几章、符号沿用前章、开头接上文结尾引下文。`--all` 要求 workspace 与
outline 完全一致——缺章(还没 `--init`)或多出磁盘残留(改标题后改名遗留)都会拒绝开始。

**注意**:实验模式(`PAPER_MODE=experiment`,默认)下 `--progress` 是唯一完整路径——
`--direct` / `--stream-raw` / 默认规划者路径是综述(survey)专用,实验模式会拒绝。

**跑起来先看这两行**:
```
[Manager  ] route      | type=method (from brief.md) | evidence=idea | number-gate=advisory | sections=1:method
[Manager  ] write-mode | full — 整篇第 4/7 章,可跨章引用
```
第一行是取材路由,第二行是写作路由。**和你的预期不符就改 `outline.md` 或 `brief.md`,
不要改代码。**

---

## 三、流水线做什么(每步产物)

```
         路由解析   → 章节位置 + (取材源, 门禁级别)   Python,不花 token
Stage 0a 备料       → input.md                  从 bibliography 挑选本章文献素材
Stage 0b 证据挖掘   → evidence-pack.md         多视角提问,视角随类型切换(idea.md 全文 + data-index.md)
Stage 1a 规划       → draft-v1.plan.md          施工图 + 术语表
Stage 1b~ 分段起草  → draft-v1.part-N.md        段数 = min(小节数,3),每段按自己的小节类型路由
          拼接     → draft-v1.md               Python 按本次实际段数拼
          数字门禁  → number-check.md           数字对账(直读 data/),严格度按类型分级
Stage 2  评审       → review-v1.md + .json      打分 + MUST FIX + needs_citation,判据随类型
          引用补全  → citation-insertions.md    插 \cite,选不到的交人工
Stage 3  收敛循环   → draft-v2.md               冻结 MUST FIX,≤4 轮修到清
Stage 4  定稿       → final.md + final.zh.md    数字门禁(覆盖 number-check.md)
Stage 5  跨章交接   → cross-chapter-state.md    把本章术语与结论交给下一章
```

> `--init` 时若 `data/data-index.md` 不存在,规划者会读 `data/` 生成它(data 类章节
> 写数字时的导航);`idea.md` 每个阶段直接读全文,不再有任何聚合资料文件。

**断点续跑**:任何一步产物已存在就跳过。想重跑某步,删掉对应文件即可。

**改了 type 怎么办**:证据路由(类型/小节类型)写进 `brief.md` 首行的 outline 指纹。改了 type
而旧产物还在,流水线会**硬停**并列出所有按旧路由生成的产物(含 `brief.md`、`evidence-pack.md`、
`draft-v1.md`、`review-v1.json`、`final.md` 等)。**正确做法**:备份需要保留的内容后,删掉列出
的陈旧产物再重跑。**不要只跑 `--init --force`**——那只会刷新 `brief.md` 的指纹,而下游的
evidence-pack / plan / review / final 仍是旧路由的,会被"存在即跳过"静默复用,等于白改。

---

## 四、编译成 PDF

```bash
python latex/build.py --paper 你的论文名   # 引用门禁 + 编译
python latex/build.py --check-only        # 只查引用闭合,不编译
```

`build.py` 编译前查 `\cite` 是否都有 bib 条目,编译后扫日志的 undefined,有问题非零退出。

---

## 四.5、skill 机制

框架把写作/图表/公式/审稿规范存成**独立 skill 文件**,Agent **需要时才按需读取**(不占用上下文)。

```
skills/
├── writing-style.md              起草者起草时:TEEL段落/禁词表/时态/hedging
├── figure-table-placeholder.md   起草者起草时:图表占位符(不生成图,写说明)
├── math-formula.md               起草者写公式:符号首次定义/全文唯一/LaTeX语法
├── review-rubric.md              评审者评审:三要素/数据结论mismatch/防谄媚
└── experiment-writing.md         起草者起草:数字纪律/章节边界/防幻觉铁律
```

- 提取自 `D:\workspace\paper-skill` 的 academic-research-skills + AI-Research-SKILLs 精华
- Agent 通过 read_file 加载对应 skill,不内联全文
- 图表占位符:Agent **不生成图**,只写 `[FIGURE N: 标题] + Note + 数据来源 + 支撑论断`,图由人工画

---

## 五、常见问题

| 现象 | 原因 | 解决 |
|---|---|---|
| `INVALID_API_KEY` | key 错 / 模型名与接口不匹配 | 核对 .env 的 key 和 base;混了前缀就统一 |
| `404 page not found` | `anthropic/` 前缀但 base 带了 `/v1` | base 去掉 `/v1` |
| `reasoning_effort` 报错 | `openai/` 前缀但模型要 Anthropic 参数 | 换 `anthropic/` 前缀 |
| `FAIL \| data-family chapter ... no experiment results` | `type:` 是 results/experiments 类但 `data/` 空 | 放结果再跑;或先跑 idea 类章节(方法章不需要数据) |
| `FAIL \| idea-family chapter ... no idea document` | 没写 `idea.md` | `copy idea.example.md idea.md` 后填内容 |
| `FAIL \| idea document ... is still the unfilled template` | `idea.md` 存在但没填内容 | 填 `idea.md` 的第 1、3、4 节(一句话贡献 / 核心洞察 / 方法设计) |
| 方法章被写成结果复述 | 那一章没声明 `type:`,退到了 mixed | 在 `outline.md` 里给这一章加 `type: method`,然后**删掉这一章的旧产物**(`brief.md` 及其下游)再重跑 |
| 审稿老要求补统计显著性(方法章) | 同上,类型没声明对 | 同上。idea 类章节的审稿判据明确禁止索要实验数字 |
| `STALE \| 下列产物按旧路由生成` 然后停了 | 改了 `type:`,旧产物还在 | 按提示删掉列出的文件再跑(含 brief.md)。一个文件都不会被自动删;别只跑 `--init --force`——那只会刷新 brief 指纹,下游旧产物仍被复用 |
| `FAIL \| brief.md 不是 --init 生成的(缺少 outline 指纹)` | brief 被手改过或非生成式 | `python run.py --init --force` |
| `stale-brief` / `FAIL \| outline.md 已改动` | 改了 outline 没刷新 brief | 备份后删掉列出的陈旧产物(含 brief.md)再跑 |
| `FAIL \| <章> 不是合法章节 … 不在 outline.md 里` | 该目录不在 outline(改了标题导致文件夹改名,遗留旧目录) | 改回标题;遗留目录可删 |
| `FAIL \| cross-chapter-state.md 不存在` | 删过那个文件 | `python run.py --init` 重建 |
| `FAIL \| Stage 5 没在 ... 留下标记` | 跨章交接没写成 | 检查 `workspace/cross-chapter-state.md` 的三个 `## ` 标题是否完好,然后重跑本章(只会重跑 Stage 5) |
| `Error: 无法从 outline.md 得出章节顺序` | `outline.md` 写坏了,或 workspace 与 outline 不一致 | 修 outline;缺章跑 `--init`,多余遗留目录清理 |
| 每章都重新定义一遍符号 | 跨章状态是空的(前面几章的 Stage 5 没跑成) | 看 `--list` 里哪些章还不是 done;`--all` 会从没交接成功的那章继续 |
| 卡在某 stage 超时 | 中转站慢 | 重跑(断点续跑会跳过已完成的) |
| `evidence-pack` 大量 "I cannot answer" | idea 章:`idea.md` 写太简;data 章:数据缺 description/hardware | 补 `idea.md` 的方法设计节;或在 final_info.json 补文本字段 |
