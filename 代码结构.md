# 代码结构 — 每个代码文件干什么、互相怎么调

> 本文是给**读代码 / 改代码 / 定位 bug** 的人看的地图。16 个 `.py` 文件各干什么、
> 谁调谁,一目了然。
>
> 分工:**流程每步干什么**见 `工作流总览.md`;**每份产物 md 是什么**见 `文档说明.md`;
> **怎么跑命令**见 `启动说明.md`;**项目是什么 / 为什么这么设计**见 `README.md`。
> 本文只讲代码文件。

---

## 一、一张总表

| 文件 | 一句话职责 | 被谁调 | 它调谁(关键) |
|---|---|---|---|
| `run.py` | CLI 入口,把命令分发给各函数 | 你(`python run.py ...`) | `agents/` 全家 + `config` |
| `config.py` | 所有路径 / 模型 / 模式 / 常量配置 | 几乎所有人 import | (无,纯配置) |
| `agents/__init__.py` | 包的对外出口,导出 Agent 组装与四个流水线入口 | `run.py` | `agents.py` / `orchestrator.py` |
| `agents/agents.py` | 用 smolagents 把三个 Agent + 五个工具装出来 | `run.py` | `prompts.py` / `tools.py` / `config` |
| `agents/prompts.py` | **三份系统指令** + 实验模式补丁(提示词的"长期性格") | `agents.py` | (无,纯常量字符串) |
| `agents/orchestrator.py` | 流水线总调度(最大文件,所有 Stage 在这) | `run.py` 单章/全篇 | `outline.py` / `chapter_type.py` / `evidence_mining.py` / `number_gate.py` / `citation_supplement.py` |
| `agents/outline.py` | outline 解析 + 工作区/跨章状态生成 + 路由指纹 | `run.py` --init、`orchestrator` | `chapter_type.py` |
| `agents/outline_expand.py` | `--expand` 展开 + data-index 生成提示 | `run.py` --expand/--init | `outline.py` / `content_source.py` |
| `agents/chapter_type.py` | 类型路由:`type:` → 取材源 family + 门禁级别 gate | `orchestrator` / `outline` / `evidence_mining` | (无,纯映射) |
| `agents/content_source.py` | idea / data 的纯读取(供门禁和导航用) | `orchestrator` 预检、`outline_expand` 生成 data-index | `config` |
| `agents/evidence_mining.py` | Stage 0:备料(规划者生 input.md)+ 挖证据(起草者) | `orchestrator` Stage 0 | `config` / `chapter_type` |
| `agents/tools.py` | 五个 `@tool`:read/write/list/search_references/search_literature | 三个 Agent 运行时调 | `retrieval.py` |
| `agents/retrieval.py` | 两层检索:本地 bib(可引)+ 网页 LLM(仅线索) | `tools.py` 的 `search_literature` | `config` |
| `agents/citation_supplement.py` | 缺引用自动补 `\cite`,带 URL 验证,不编造 | `orchestrator` Stage 2 之后 | `retrieval.py` |
| `agents/citation_check.py` | 编译前引用闭合校验 + 日志解析 | `latex/build.py` | (无,纯标准库) |
| `agents/number_gate.py` | 正文数字对账 `data/`,防幻觉 | `orchestrator` 数字门禁两处 | `content_source`(间接,同源) |

> `latex/build.py`(编译 PDF)和 `tests/` 见末尾,不在主流水线里。

---

## 二、五个重点文件展开

### `config.py` — 一切配置的唯一来源

纯配置,无逻辑。分组:

- **路径**:`PROJECT_ROOT` / `DATA_ROOT` / `WORKSPACE_ROOT` / `REFERENCES_ROOT` /
  `IDEA_PATH`(全局创新点)/ `DATA_INDEX_PATH`(data/ 的三级索引)
- **模式**:`PAPER_MODE`(experiment / survey;本项目固定 experiment,survey 是对照残留)
- **三组模型**(各自独立 key/base):`DRAFT_*` / `REVIEW_*` / `MANAGER_*`,对应
  `get_draft_model()` / `get_review_model()` / `get_manager_model()`(各带不同 reasoning_effort)
- **收敛循环常量**:`MAX_REVISION_ROUNDS`(Stage 3 最多几轮)、`REVIEW_SCORE_THRESHOLD`
- **检索模型**:`RETRIEVAL_*`(留空则关闭网页检索,只走本地 bib)

改任何路径、模型、阈值都在这里,代码里不硬编码。

---

### `agents/prompts.py` — 提示词的"长期性格" ⭐

> **先说清一件事**:真正决定每阶段"读什么 / 写什么 / 怎么改"的,是
> `orchestrator.py` 里每个 Stage 当场拼的那段 prompt(含 `idea_clause`、路径、
> must_fix 清单等)。**这里的 `prompts.py` 不是那个**。
>
> `prompts.py` 装的是三个 Agent 的**系统指令(system instructions)**——它们一出生就
> 带着的"我是谁、守什么纪律",在整条流水线里**恒定不变**。是性格,不是任务。

四块:

1. **`GLOBAL_INSTRUCTIONS`** — 三份指令共用的纪律:不编数据/引用、evidence 分级、
   合并优先级、目录规矩、必产物文件、不要把草稿打印到终端。
2. **`DRAFT_INSTRUCTIONS`** = GLOBAL + 起草者专属(句/段/章三层写作规范、AI 腐蚀词黑名单、
   跨段术语表遵守、实验论文数字纪律、初稿/改稿两套工作流)。
3. **`REVIEW_INSTRUCTIONS`** = GLOBAL + 评审者专属(7 维打分、MUST/SHOULD/CONSIDER 分级、
   `review-v1.json` 的 schema、VERIFY 模式、Finalization 模式)。
4. **实验模式补丁**(不烤进 Agent,由 orchestrator 按需注入任务 prompt):
   `ANTI_HALLUCINATION_TIPS`(数字铁律)、`SECTION_TIPS`(按 section `type` 选条的写作指引)、
   `EXPERIMENT_DRAFT_ADDENDUM` / `EXPERIMENT_REVIEW_ADDENDUM`(把"这是实验论文"加进任务)。

被 `agents.py` 在组装三个 Agent 时 import(`DRAFT/REVIEW/MANAGER_INSTRUCTIONS`),
测试 `test_pipeline_routing.py` 也直接断言其中片段。

---

### `agents/orchestrator.py` — 流水线总调度(1604 行,最核心)

整个项目唯一"知道流程怎么走"的地方。按职责分块:

| 块 | 关键函数 | 作用 |
|---|---|---|
| 进度打印 | `print_stream_progress` / `ProgressFilteringStdout` | 过滤 smolagents 的噪音,只留 `[Manager ] Stage N` 这种 |
| 路由门禁 | `warn_stale_route_artifacts` | brief 指纹 ≠ outline → 硬停,列出陈旧产物 |
| **idea 注入** | **`idea_clause(family)`** | 返回提示词第一行:"先读 idea.md(+ 数据章另读 data-index.md)" |
| 跨章校验 | `cross_chapter_state_has_claim` / `cross_chapter_state_preserves_others` | Stage 5 候选文件的两条 Python 校验 |
| 分段 | `build_stage1_parts` / `part_family` / `concatenate_stage1_parts` | 段数 `min(小节数,3)`,每段按自己 type 路由 |
| 收敛循环 | `run_convergence_loop` | Stage 3:起草者改 ↔ 评审者验,冻结 must_fix,≤4 轮 |
| 工具辅助 | `run_agent_stage_standalone` | 不走主循环的单次调用(--expand、data-index 生成用) |
| **主流水线** | **`run_4stage_with_progress(draft, review, folder, manager)`** | **逐章主循环**,实验模式唯一走的入口 |

`run_4stage_with_progress` 内部定义了**嵌套闭包 `run_agent_stage(agent, prompt)`**——
它才是"把拼好的提示词喂给指定 agent"的那一层。Stage 0a/0b/1a/1b/2/3/4/5 全经由它调用。

> 另有三个**残留给 survey 模式**的入口(`run_4stage_via_manager` / `..._stream` /
> `run_4stage_direct`),实验模式下被拒绝,留着供对照。

---

### `agents/outline.py` — outline 解析与工作区生成

- `parse_outline()` — 把 outline 文件(`##` 章 / `###` 小节 / 表格骨架两种写法)解析成章节清单;`--expand` 读 `outline_draft.md`,`--init` 读定稿 `outline.md`
- `chapter_fingerprint(chapter)` — 章节的 sha256 指纹,盖进 brief 首行;改 type 不重 `--init`
  → 指纹不符 → orchestrator 硬停(就是上一节那条路由门禁的依据)
- `render_brief()` / `read_brief_fingerprint()` — 生成 brief.md、读回它的指纹
- `init_chapter_workspaces()` — `--init` 的核心:为每章建 `workspace/<NN-type>/brief.md` +
  `cross-chapter-state.md`
- `resolve_write_mode()` / `build_outline_excerpt()` / `build_mode_clause()` — 算本章是第几章、
  邻章是什么,拼成"写作契约"塞进提示词

`DEFAULT_SECTION_WORDS=250`、`DEFAULT_WORDS_BY_TYPE`(按 type 的默认词数)也在这。

---

### `run.py` — 命令到函数的映射

| 命令 | 调的函数 |
|---|---|
| `--init [--force]` | `_translate_zh_to_en()`(中文版新则翻译覆盖英文)+ `init_workspaces()` → `outline.init_chapter_workspaces()` + `_maybe_build_data_index()` |
| `--expand` | `expand_outline()` → `outline_expand.build_expand_prompt()` 调规划者,生成中英双份 |
| `--all --progress` | `run_all_chapters()` → 逐章 `run_4stage_with_progress()` |
| `<章名> --progress` | 直接 `run_4stage_with_progress()` |
| `--list` | `list_paper_folders()` |

`_maybe_build_data_index()` 在 `--init` 末尾:若 `data/` 有内容且 `data-index.md` 不存在,
调规划者生成它(存在则跳过,不覆盖)。

---

## 三、调用骨架(命令怎么流到 agent)

```
你敲 python run.py --all
  └ run.py: run_all_chapters()
       └ 逐章 orchestrator.run_4stage_with_progress(draft, review, folder, manager)
            ├ 前置门禁  (outline.resolve_write_mode + chapter_type 路由 + content_source 预检)
            ├ Stage 0a  evidence_mining.run_input_material(manager)  → input.md
            ├ Stage 0b  evidence_mining.run_evidence_mining(draft)   → evidence-pack.md
            ├ Stage 1a  run_agent_stage(planner,  idea_clause + 规划提示词) → draft-v1.plan.md
            ├ Stage 1b  run_agent_stage(draft ×N,  idea_clause + 分段提示词) → draft-v1.md
            ├ 数字门禁  number_gate.run_number_gate()  → number-check.md
            ├ Stage 2   run_agent_stage(review, 评审提示词) → review-v1.md + .json
            ├ 引用补全  citation_supplement.apply_insertions() → 改 draft-v1.md
            ├ Stage 3   run_convergence_loop(draft↔review) → draft-v2.md
            ├ Stage 4   run_agent_stage(review, 定稿提示词) → final.md + final.zh.md
            └ Stage 5   run_agent_stage(review, 交接提示词) + Python 校验 → cross-chapter-state.md
```

`run_agent_stage(agent, prompt)` 是 `run_4stage_with_progress` 里的嵌套闭包:接 Python 拼好的
提示词 → 调对应 agent → agent 用 `tools.py` 的工具读写文件 → Python `verify()` 校验产物存在。

---

## 四、不在主流程的两个

- **`latex/build.py`** — 编译 PDF 用,不经任何 Agent。调 `agents/citation_check.py` 的
  `run_citation_gate` 编译前后查引用闭合。
- **`agents/citation_check.py`** — 纯标准库,只在编译时被 `build.py` 调;主流水线不碰它。
  (主流水线的引用补全是 `citation_supplement.py`,名字像但不是同一个。)

---

## 五、tests/(离线,不调 API,假 Agent 驱动)

| 文件 | 测什么 |
|---|---|
| `test_routing.py` | 类型解析、别名、分段路由、判据切换 |
| `test_outline.py` | outline 解析、brief 往返、默认字数查表、写作契约 |
| `test_pipeline_routing.py` | 假 Agent 跑完整流水线,验证门禁接线 + idea_clause 注入 |
| `test_optimizations.py` | 分段自适应、指纹、工具链 |
| `test_expand.py` | `--expand` 的越权改结构校验 |

改代码后跑这五套确认接线,不花 token。
