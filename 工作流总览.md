# 实验论文多 Agent 写作 — 完整工作流

> 本文档描述整个系统的多 Agent 工作流程:每个阶段读什么、产出什么、谁执行。
> 每份文档的职责见文末「工作流里的每份文档」。
>
> 配套文档:`README.md`(项目总览与快速开始)、`文档说明.md`(文件清单与交付状态)、
> `data/README.md`(实验结果的数据契约)、`idea.example.md`(创新点文档模板)、
> `outline.example.md`(论文总纲模板)。

---

## 三份人工输入,各管一件事

框架跑起来靠三份你写的文件,职责严格分开,互不重复:

| 文件 | 位置 | 管什么 | 谁读 |
|---|---|---|---|
| `idea.md` | 项目根 | **贡献是什么**:一句话贡献、核心洞察、方法设计、与前人的 delta | Python 装进 context-pack |
| `outline.md` | 项目根 | **结构是什么**:整篇分几章、每章什么 `type:`、什么顺序、各节多少字 | Python 生成各章 brief |
| `data/` | 项目根 | **数字是什么**:实验结果(CSV/JSON/日志/图) | Python 装进 context-pack |

各自都是**唯一事实来源**:贡献只从 `idea.md` 取,结构只从 `outline.md` 取,数字只从 `data/` 取。
框架不替你编任何一项——缺了就明确报错或标记,不"合理地"补一个。

`brief.md` 不是第四份输入。它是 `outline.md` 的**生成产物**(每章一份),见下文。

---

## outline 的两段式:骨架你写,细节 Manager 补

`outline.md` 里其实混着两类信息,该由不同的人来定:

| 信息 | 谁定 | 为什么 |
|---|---|---|
| 分几章、每章叫什么、什么 `type:`、什么顺序 | **你** | 这是你对自己工作的判断,框架没有默认章节模板 |
| 每章拆哪几个小节、每节写什么、边界在哪 | **Manager** | 从 `idea.md` 推导出来的施工细节,正是规划该干的活 |
| 每节多少字 | **你** | 篇幅是投稿约束(会议页数、章节配比),Manager 不知道 |

所以流程是三步,中间夹一道你的审阅:

```
outline.md(填表格)  ──run.py --expand──►  outline.expanded.md  ──你审 + 补字数 + 改名──►  outline.md  ──run.py --init──►  各章工作区
   章标题 + type              Manager 读 idea.md            小节 + 起草要点
```

**跳过 `--expand` 直接 `--init` 会被拒绝。** 只有章标题和 `type:` 的 outline,生成的 brief
里一个小节都没有;起草会退回单段、拿不到任何要点,Draft 只能照章标题硬编,而流水线
全程显示成功。这道拦截在建工作区之前、调模型之前。

### 第一步:填表格

`outline.example.md` 复制成 `outline.md`,只需替换一张表里的占位符:

```markdown
| # | 章标题 | type |
|---|--------|------|
| 1 | Abstract      | abstract |
| 2 | Introduction  | intro    |
| 3 | Related Work  | related  |
| 4 | 频域通道注意力 | method   |
```

加减行即可,章标题中英文都行(决定文件夹名)。`type` 从词表取值(见「章节类型路由」)。
表格骨架**只在文件里没有任何 `##` 章标题时生效**——`--expand` 一跑就变成 `##` + `###` 语法,
那之后文件里的表格(比如你自己记的备注表)不会再被当成章节声明。

各列按**表头**识别而不是按位置,所以列序可换、可加备注列;没替换的占位符行整行跳过。

### 第二步:`--expand`

```bash
python run.py --expand
```

Manager 读 `idea.md`,为每章写 2~4 个小节,每节 2~5 条起草要点。要点里最值钱的一条是
**边界**——说清哪部分内容属于邻章、本节不许碰(「只讲机制,任何实测数字归结果章」)。
跨章重复与范围蔓延是这套流水线最常见的失败,而起草 Agent 自己判断不出边界。

它**不许**碰你的结构。四道校验在采纳前跑:

| 越权行为 | 处理 |
|---|---|
| 增删、合并、拆分、重排、改名章节 | 拒绝采纳,文件留在原地供你查看 |
| 改任何一章的 `type:` | 同上 |
| 某章没展开出小节 | 同上 |
| 写了字数标注 | 提示词里禁止;字数由你补 |

产物落在 `outline.expanded.md`,**从不原地覆盖** `outline.md`:Manager 的判断要你过一遍再采纳。
上一轮的 `outline.expanded.md` 还在时 `--expand` 直接拒绝跑——留着它,本轮 Manager 写失败会被
"文件存在即成功"的判断读成上一轮的内容,你拿着一份陈旧展开去 `--init`,而终端显示成功。

### 第三步:审阅、补字数、改名

读 `outline.expanded.md`,改掉不认同的小节与要点,在小节标题后补 `(~N words)` 定篇幅:

```markdown
### 4.1 总体框架 (~350 words)
```

不标字数的小节按默认 250 词处理,`--expand` 跑完会告诉你还有几个没标。然后:

```bash
mv outline.expanded.md outline.md
python run.py --init
```

改名这一步由你做,不由框架代劳。`outline.md` 是结构的唯一事实来源,而 `--expand`
的产物需要你过一遍才算数 —— 用两个文件名区分「待审」和「已采纳」,状态一眼可见。

> 不想用 `--expand` 也行:自己把 `###` 小节与要点写进 `outline.md`,`--init` 只认这一个文件。

---

## outline.md 与 brief.md 的关系

`outline.md` 是整篇目录(骨架你写、小节由 `--expand` 补);`python run.py --init` 把它拆成每章一个工作区:

```
outline.md  ──run.py --init──►  workspace/04-method/brief.md   (本章规格)
(整篇结构)                       workspace/04-method/input.md    (素材,待你填)
                                 workspace/cross-chapter-state.md (跨章状态,整篇模式专用)
```

**为什么保留 brief.md 而不让流水线直接读 outline**:

- **上下文分层**。Agent 只该看到自己这一章的规格,不该把整篇目录塞进每次调用。
- **自包含工作区**。`workspace/04-method/` 带着一份 brief,能看出这次运行依据的是什么规格,
  不必回溯 outline 的历史版本。

代价是两份文件可能不同步。所以生成的 brief 首行盖一行 outline 指纹(HTML 注释,渲染时不可见):
改了 outline 却没重新 `--init`,运行时直接**硬停**并要求你 `--init --force` 刷新,
不会拿旧规格继续跑。指纹用 sha256 算,跨进程稳定。

`--init` 默认**不覆盖**已存在的 brief.md(你可能手改过),只列出"已过期"让你确认;
`input.md` 即使 `--force` 也永不覆盖——那是你的素材。

---

## 两种写作模式(两条路由)

同一套流水线,但对 Agent 的要求**相反**,所以是两条独立路由、两套指令。

### 整篇模式 FULL — 写一整篇论文

```bash
python run.py --expand               # 章节骨架 → Manager 补小节与要点 → outline.expanded.md
                                     #   审阅它,补 (~N words),改名成 outline.md
python run.py --init                 # outline.md → 各章工作区 + 跨章状态
python run.py --all --progress       # 按 outline 顺序跑全部,失败即停
python run.py 04-method --progress   # 也可以只跑其中一章
```

这一章是整篇里的第 N 章:
- 知道自己是第几章、前后是什么(注入一段**有界**的邻章结构摘要,只给规划者)
- 允许跨章引用,但必须按名字引(`the method chapter`),不许猜章号
- **不重复定义**前面章节已固定的符号——从 `cross-chapter-state.md` 照抄
- 开头接上文、结尾引下文;首章只开篇,末章只收尾
- 跑完后 **Stage 5** 自动把本章的术语约定与一句话结论追加进跨章状态

### 逐章模式 SINGLE — 只写单独一章

```bash
mkdir -p workspace/my-chapter        # 自己建文件夹
# 放一份 brief.md(格式照 workspace/_TEMPLATE/brief.md)
python run.py my-chapter --progress
```

这一章要**自包含**:
- 所有术语、缩写、符号在本章首次出现处定义——没有上一章可以继承
- **严禁**写 `as shown in Section 3`、`we introduced X earlier`、`the next chapter evaluates`
  这类过渡句:那些目标不存在,是悬空引用
- 不假设前后章覆盖什么;需要的背景就在本章用一两句说清
- 不读也不写跨章状态。**即使那个文件存在**(同一 workspace 下有整篇的章),也不给它路径
- 不跑 Stage 5

### 路由判据

**只有一条:该文件夹在不在 `outline.md` 里。**

不看命令行参数——`--all` 和单章命令都可能跑同一个文件夹,决定"这章有没有邻章"的是结构文件,
不是这次怎么启动的。所以:把手建的章补进 `outline.md` 再 `--init`,它就转成整篇模式的一章。

`python run.py --list` 会标出每个文件夹走哪条路由;单章运行开跑前也会先打印路由说明,
让你在花 token 之前就能确认。

### 为什么必须分开

用同一份指令跑两种模式的后果是**双向**的:整篇模式下每章都自我重复定义一遍符号;
逐章模式下每章都写着"承接上一章"而上一章并不存在。

写作契约(mode clause)注入了**每一个会产出或改写正文的阶段**:Stage 1a 规划、
Stage 1b/1c 各起草段、Stage 3 收敛循环的每一轮、Stage 4 定稿。漏掉任何一个,
那一步就会按"没有契约"写,把前面按契约写好的部分改回去,而流水线全程显示成功。

审稿侧同样分流:自包含单章里,审稿人不会要求"补上与前一章的衔接"(否则收敛循环把
首轮 must_fix 冻结成验收单,接下来几轮都在造悬空引用)。

### 路由变更的保护

写作模式(FULL/SINGLE)与证据路由(type/family/gate)一起写进 `context-pack.md` 首行的
指纹。路由变了而目录里还留着旧路由的产物时,流水线**硬停**:

- **一个文件都不删**——那些产物是真金白银的 token,留不留由你决定
- 但也**绝不继续**:继续会覆盖指纹,警告下一次就不再出现,而旧路由的 plan / part /
  review / final 会被"存在即跳过"静默复用,最后 Stage 5 还会把它们的术语写进跨章状态

另有四道 brief 来源门禁,全部在调模型之前硬停:

| 情况 | 为什么拦 |
|---|---|
| 整篇的章配一份手写 brief | 手写 brief 没有章序号与结构约定,按整篇跑会写错衔接 |
| 整篇的章 brief 指纹与 outline 不符 | 用的是旧章节规格 |
| 带生成指纹的目录不在当前 outline 里 | 大概率是改了章标题导致文件夹改名,旧产物是按整篇写的 |
| 整篇路由但跨章状态文件不存在 | 本章无处读前章术语、也无处写自己的约定 |

`outline.md` 存在但解析不出任何章节、或两章映射到同一文件夹,同样硬停——
不确定是整篇还是逐章时,两种指令都可能是错的,不猜。

---

## 章节类型路由(决定从哪儿取材)

**论文的贡献是创新点和原理,实验数据只是支撑证据。** 所以有两个内容源,
由 `brief.md` 顶部一行 `type:` 分流(`outline.md` 里每章的 `type:` 会写进生成的 brief):

```
outline.md 每章的 type:  ──►  brief.md  ──►  agents/chapter_type.py  ──►  (family, gate)
                                                                          │        │
                                                    取材源 ◄──────────────┘        └──► 数字门禁级别
```

| family | 主输入 | 结果表的角色 | 适用类型 |
|---|---|---|---|
| `idea` | **`idea.md`** | 辅助证据,最多引一个 headline 数 | abstract, intro, related, background, method, theory |
| `data` | **`data/` 结果库** | 就是内容本身 | experiments, results, analysis, ablation |
| `mixed` | 两者并重 | 支撑论点 | discussion, limitations, conclusion |

| gate | 行为 | 适用 |
|---|---|---|
| `blocking` | `data/` 为空 → **拒绝起草**(这类章节的内容就是数字) | data 类 |
| `advisory` | 有结果就比对,没结果只提示 UNVERIFIED,不拦 | idea / mixed 类 |
| `off` | 不跑门禁(纯论述章) | related, background |

**贯穿四个环节**——证据挖掘视角、起草主输入、审稿判据、门禁严格度。方法章的审稿不会索要
统计显著性,结果章的审稿不会索要方法阐述。

**分段路由**:一份 brief 覆盖多种类型的小节时,各起草段按**自己覆盖的小节**路由。例如
Abstract+Intro 段走 idea、Results+Conclusion 段走 mixed,不是整章一刀切。

**两处 fail-closed**:
- idea 类章节 + 无 `idea.md`(或 `idea.md` 还是未填的模板)→ 拒绝起草
- data 类章节 + 无 `data/` → 拒绝起草

写错的 `type:` 退回 mixed/advisory 并在终端提示合法值;不写会尝试从文件夹名推断
(`05-results` → results),仍推不出就 mixed/advisory 并提示补。

---

## 系统架构

```
┌──────────────────────────────────────────────────────────────────┐
│                    Manager Agent (CodeAgent)                       │
│              模型：GPT-5.5 (reasoning_effort=high)                 │
│              职责：outline 展开(--expand)、Stage-1a 规划、术语表    │
├──────────────────────────┬───────────────────────────────────────┤
│                          │ managed_agents=[draft_agent, review_agent]
│    ┌─────────────────────┴────────────────┐                      │
│    ▼                                      ▼                      │
│  Draft Agent (CodeAgent)            Review Agent (CodeAgent)      │
│  模型：Claude Opus 4.6                 模型：GPT-5.5              │
│  reasoning_effort=max                  reasoning_effort=high      │
│  职责：证据挖掘、起草、修订             职责：评审、VERIFY、定稿、Stage 5 │
└──────────────────────────────────────────────────────────────────┘
```

**编排由 Python 做,不由 Manager 做。** Manager 只承担两处规划:`--expand` 的 outline 展开
(流水线之外的一次性调用)和 Stage 1a 的段规划(它需要全景才能划清段边界)。
阶段推进、门禁、断点续跑、重试全部是 `orchestrator.py` 里的确定性代码。

**工具**(五个共享,所有 Agent 通过文件系统通信,不直接对话):
- `read_file` / `write_file` / `list_folder`(文件 I/O,硬拦 index.md 全表)
- `search_references`(有界检索本地文献索引,上限 20 行)
- `search_literature`(两层检索:本地 bib + 可选网页 LLM;web 命中标为"线索,不可引")

**内容源(由 `config.py` 的 `PAPER_MODE` 决定)**:
- `experiment`(默认):`idea.md` 创新点文档 + `data/` 实验结果库
- `survey`:参考文献笔记库(与综述引擎对齐,供复用/测试)

---

## 章节工作区

```
idea.md                        (人工) ⭐ 全局创新点,所有章节共读同一份
outline.md                     (人工骨架 + --expand 补小节) 整篇结构,--init 的输入
outline.expanded.md            (--expand 产物) 待你审阅、补字数、改名成 outline.md
data/                          (人工) 实验结果库
workspace/
├── cross-chapter-state.md     (--init 生成 + Stage 5 追加) 整篇模式专用
├── 01-abstract/               (--init 生成)
│   ├── brief.md               (生成) 首行 outline 指纹 + type: + 小节规格
│   ├── input.md               (生成骨架,你填素材) 永不被覆盖
│   └── ...                    (Agent 与 Python 的产物)
├── 04-method/
├── my-chapter/                (你自己建 → 逐章模式)
│   ├── brief.md               (手写)
│   └── input.md               (手写)
└── _TEMPLATE/brief.md         (模板) 手建单章时照它写
```

`idea.md` 是**全局一份**(可用 `.env` 的 `IDEA_PATH` 改位置),所有章节都读同一份——
这样创新点的表述不会前后章漂移。`data/` 结果库是数字的唯一事实边界。

写方法章**不必等实验跑完**:`type: method` 的门禁是 advisory,`data/` 为空也能起草。

---

## 流水线总览

```
Stage 0    Stage 1a    Stage 1b~1c   拼接      Stage 2    Stage 3        Stage 4   Stage 5
证据挖掘    规划        分段起草       (Python)   评审       收敛修订        定稿      跨章交接
Draft      Manager     Draft ×N                Review     Draft↔Review   Review    Review
                                                                                   (仅 FULL)
```

段数**自适应**:`min(小节数, 3)`,没有小节时退回单段。Abstract 这类只有 1-2 节的章不会被硬凑
成三段(那会生出没有来源的空段,并给它们不合理的字数目标)。

`--progress` 是唯一支持的模式(实验模式下强制)。Python 控制进度、可断点续跑、
瞬时错误自动重试 ×4。`--direct` / `--stream-raw` / 默认 Manager 路径是 survey 专用,
在实验模式下会被显式拒绝——它们会绕过内容源、数字门禁和收敛循环。

**断点续跑**:每个产物"存在即跳过"。这也是路由指纹机制存在的原因——
改了路由却复用旧产物,是这套设计里最隐蔽的失败模式。

---

## 各阶段详细说明(读什么 / 输出什么)

### 前置:路由解析 + 内容源装配(Stage 0 之前,Python)

**执行者**:Python(`outline.py` + `chapter_type.py` + `content_source.py`,不耗 token)

**输入**:
- `outline.md`(判定 FULL/SINGLE、算邻章、算 brief 指纹)
- `brief.md` 的 `type:` 声明 + 各小节的 `- type:` 覆盖
- `idea.md` / `data/`

**工作流**:
1. **写作路由**:该文件夹在不在 outline 里 → FULL / SINGLE。构造两套 mode clause
   (起草侧 + 审稿侧)。FULL 另加一段有界的邻章结构摘要
2. 四道 brief 来源门禁(见上文表格),任一不符直接硬停
3. **证据路由**:解析 `type:` → (family, gate),小节级取并集(有 data 小节则整体 blocking)
4. **两处 pre-flight fail-closed**:idea 类缺 `idea.md`(或还是未填模板)、data 类缺 `data/`
   → 拒绝起草,**不调模型**。两个主输入都在这里查完,不等跑到后面的门禁才发现
   (那要白烧四次调用)
5. 比对 `context-pack.md` 首行的路由指纹;不一致就列出陈旧产物并硬停(不删文件)
6. 按 family 装配 `context-pack.md`:idea 章把 `idea.md` 排最前标 PRIMARY、结果表标
   SUPPORTING;data 章反过来;mixed 章两者并重。首行盖上路由指纹
7. 这一步**装配失败就停**(fail-closed):打印 FAIL、记 `context_pack_error`,不进任何
   Agent 阶段。继续跑的话 Agent 会读到上一次的旧 pack、或一个不存在的路径,写出的正文
   基于错误上下文而流水线显示成功

**输出**:`context-pack.md`(带指纹的、按类型排序的证据包)

**终端**:`[Manager] route |` 与 `[Manager] write-mode |` 两行打印解析结果。
和预期不符就改 `outline.md` / `brief.md`,不改代码。

---

### Stage 0:证据挖掘(实验模式才有)

**执行者**:Draft Agent

**输入**:`context-pack.md`(按类型排序)+ `brief.md`

**工作流**:
1. 按 4 个审稿视角各提 3-5 个硬问题。**视角随 family 切换**:
   - idea 章:新颖性 / 机制(为什么 work)/ 精确性(能否复现实现)/ 替代设计
   - data 章:统计显著性 / baseline 与消融 / 可复现性 / 过度声称
   - mixed 章:论断-证据对应 / 机制是否被证实 / 过度声称 / 局限
2. 逐问作答。**grounding 规则也随 family 变**:idea 章必须引用作者原话,
   data 章必须引用具体指标值。答不出就写 "I cannot answer from the provided evidence"

**输出**:`evidence-pack.md`(多视角 Q&A + 末尾 Open Gaps 清单)

**不做**:不写论文正文;不编数字,不编机制。

---

### Stage 1a:规划(Manager)

**执行者**:Manager Agent(无 Manager 时退回 Draft)

**输入**:
- `brief.md`(本章规格)/ `input.md`(素材)
- `context-pack.md`(按类型排序的证据)
- `evidence-pack.md`(**读全文**——它是规划的证据基础)
- 证据路由子句(哪个源是主输入)
- **写作契约子句**(FULL:第几章/可跨章引用/复用前章符号;SINGLE:自包含/禁跨章过渡)
- FULL 专属:邻章结构摘要 + `cross-chapter-state.md` 的路径
- Python 算好的段边界(不许移动小节)
- filtered reference excerpt(有界文献)

**工作流**:
1. 读 brief 明确本章必须回答的问题
2. 读 input + context-pack 确定证据边界
3. 读 evidence-pack 全文,把每段需要的证据**浓缩进计划**(起草者读计划,不读整个 pack)
4. 写规划,开头固定一个 `## Notation and Terminology Table`——全章术语/缩写/符号冻结。
   FULL 模式下这张表要**沿用**跨章状态里已有的定义,不另立一套
5. 每段指定:target claims、required REF IDs、禁区(不许碰的内容)、过渡作用、输出文件

**输出**:`draft-v1.plan.md`(含术语表)、`todo.md`(规划已知缺口)

---

### Stage 1b~1c:分段起草(Draft ×N)

**执行者**:Draft Agent(每段一次调用)

**输入**:
- `brief.md` / `input.md` / `draft-v1.plan.md`
- **该段自己的证据路由子句**(按这段覆盖的小节算,不是整章一刀切)
- **写作契约子句**(同 Stage 1a)
- 上一段的结尾(≤1200 字符,保证段间衔接)
- 术语表(必须逐字遵守,禁止重定义前段已固定的符号)
- `evidence-pack.md`(**按需** `read_file` 读本段相关的 Q&A,不整段内联)

**工作流**(每段一次):
1. 读计划里属于自己的那段边界
2. 读上一段结尾 + 术语表,保持衔接和术语一致
3. 只写本段,不碰其他段
4. 按本段的 family 取材:
   - idea 段:从 `## Core idea` 展开机制和设计,结果表最多引一个 headline 数;
     设计细节缺失标 `[DESIGN DETAIL NEEDED]`,**不许"合理地"补一个**
   - data 段:只写结果表里的真实数字,缺的标 `[MISSING DATA]`;不重述方法设计

**输出**:`draft-v1.part-1.md` … `draft-v1.part-N.md`

**拼接(Python,无模型)**:按**本次实际段数**拼成 `draft-v1.md`。
段数是传进去的,不是硬编 3——否则上次跑三段留下的 part-3 会被拼进这次的两段草稿。

---

### 数字门禁(Stage 1 后,Python 自动,按类型分级)

**执行者**:Python(`number_gate.py`,确定性,不耗 token)

**输入**:`draft-v1.md` + `data/` 结果库 + 本章 gate 级别

**工作流**:逐个数与结果库对账(邻近度绑定 + 量纲缩放容错),抓幻觉数字

**输出**:`number-check.md`(标注 gate 级别 + PASS 或逐条 MISMATCH)

**分级行为**:
- `blocking`(data 类):结果库为空的情况已在 pre-flight 拦掉;到这里说明有数据,
  MISMATCH 注入 Stage 2 当 MUST FIX
- `advisory`(idea/mixed 类):结果库为空 → 打印 UNVERIFIED 提示后继续。有数据则照常对账
- `off`(related/background):完全不跑

---

### Stage 2:评审(Review)

**执行者**:Review Agent

**输入**:
- `draft-v1.md` / `brief.md` / `input.md` / `context-pack.md`
- **证据类型判据子句**(这章按什么标准判)
- **写作模式判据子句**(FULL:查重复定义、查跨章重复、查开合过渡缺失;
  SINGLE:未定义符号算 MUST FIX、跨章引用算 MUST FIX、不要求补跨章过渡)
- `number-check.md`(数字门禁结果)
- filtered reference excerpt

**工作流**:
1. 读 draft-v1 + brief 确认是否回答了该回答的问题
2. **按证据类型套判据**:
   - idea 章:机制是否只是断言而未解释?设计选择有没有理由?有没有跑偏成结果复述?
     **明确不许索要更多实验数字,缺指标不算缺陷**
   - data 章:每个数字能否对回结果表?baseline/消融是否齐全?方差与种子有没有报?
     结论是否强于数据支撑?**不许索要方法阐述**
3. **按写作模式套判据**(见上)
4. 读 `number-check.md`,把每条 MISMATCH 当 MUST FIX
5. 按 7 维打分(Accuracy / Completeness / Clarity / Structure / Readability /
   AI Traces / Style Consistency)
6. 问题分 MUST FIX / SHOULD FIX / CONSIDER
7. 写 Markdown 评审 + JSON 清单(JSON 是修订阶段的验收单)

> **为什么判据必须双重分流**:证据类型不分流,审稿会对方法章统一套实验论文标准,把
> "缺统计显著性""缺 baseline"塞进 MUST FIX;写作模式不分流,审稿会在自包含单章里要求
> "补上与前一章的衔接"。而首轮 must_fix 会被**冻结成验收单**,Draft 接下来几轮都在
> 满足这些本来就不该有的要求——或者在造悬空引用。

**输出**:
- `review-v1.md`(人类可读评审)
- `review-v1.json`(机器可读:7 维分数 + must_fix 冻结清单 + `needs_citation` 数组)
- `todo.md`

**Python 后置校验**:进收敛循环前校验 `review-v1.json` 的 schema——`must_fix` 必须是
`{id, location, issue, suggestion}` 的列表。不合法就删掉产物强制 Stage 2 重跑,
不允许"降级成一次无验收的修订"这种最糟的"通过"。

---

### 引用补全(Stage 2 之后,Python)

**执行者**:Python(`citation_supplement.apply_insertions`,确定性)

**输入**:`review-v1.json` 的 `needs_citation`(缺引用的句子 + 建议 bib key)

**工作流**:
1. 只从**你提供的** bibliography 匹配 key,把 `\cite{REF-XXXX}` 机械插进那句末尾
2. 匹配不到 key 的 → 标 needs_human 交你决定,**绝不编造**

**输出**:`citation-insertions.md`(插了哪些 / 留给人哪些)

> 分工:review 负责**判断**(标记缺引用句子 + 建议 key),Python 负责**执行**(机械插入)。
> review 不亲自改稿。

---

### Stage 3:收敛修订(Draft ↔ Review,最多 4 轮)

**执行者**:Draft Agent(修订)+ Review Agent(VERIFY)

**输入(每轮)**:
- 冻结的 must_fix 清单(只认这张单)
- 上一版草稿 + `brief.md` / `input.md` / `context-pack.md`
- **写作契约子句**(每一轮都带——收敛循环会重写整篇草稿,不带的话第 2 轮就可能把
  自包含约束改回去)

**工作流**:
1. Draft 读冻结清单,逐条在对应位置修复,保留已正确的段落
2. Review 进 VERIFY 模式:**只核验冻结清单是否解决,不新增问题**
3. 全解决 → 晋升为 `draft-v2.md`;未全解决 → 再修一轮
4. 到 4 轮仍没清 → 剩余项写进 `todo.md` / `decision.md` 交人工,不空转

**输出**:`draft-v2.round-N.md`(每轮)、`review-verify.round-N.json`(每轮核验)、
`draft-v2.md`(通过的那轮晋升)、`todo.md` / `decision.md`

---

### Stage 4:定稿(Review)

**执行者**:Review Agent

**输入**:`draft-v1.md` / `draft-v2.md` / `review-v1.md` / `brief.md` / `input.md` /
`context-pack.md` / `todo.md` + **写作契约子句**

**工作流**:
1. 合并两版最佳部分,解决所有 MUST FIX / SHOULD FIX
2. 数字必须与结果库一致
3. 无 AI 痕迹、术语一致、引用完整
4. 定稿后 Python 再跑一次数字门禁(防修订/合并引入新的错误数字)

**输出**:`final.md`(英文 publication-ready)、`final.zh.md`(中文审阅版)、
`decision.md`(取舍记录)、`todo.md`(final 数字门禁的 mismatch 也会追加到这里)

**不做**:不把两版拼成长冗文本;final 里不含评审意见。

---

### Stage 5:跨章交接(Review,**仅整篇模式**)

**执行者**:Review Agent

**为什么必须自动做**:整篇模式下 `cross-chapter-state.md` 是章节间传递术语与结论的
**唯一载体**。下一章靠它拿到"上一章把这个符号定成了什么""哪些结论已经建立"。
原先只打印一行"记得手动更新"——漏一次,后面章节的术语就开始漂。

**输入**:本章 `final.md` + `todo.md` + 现有的 `cross-chapter-state.md`

**输出**:`stage5-candidate.md`(**不直接改** `cross-chapter-state.md`)。Review 写候选,
Python 校验通过后才原子替换目标文件。这一层间接是必需的:"它把前几章的条目删了"这种
失败只能在写完之后才发现,而那时如果目标文件已经被覆盖,前章的术语约定就找不回来了。

**工作流**:按三个小节 upsert 本章的条目。每条 bullet 必须以 `- [<章节文件夹名>] ` 开头,
并替换同前缀的旧条目——所以重跑是幂等的:

| 小节 | 写什么 |
|---|---|
| `## Terminology Decisions` | 本章固定的术语拼写、缩写展开、符号定义(取自 plan 的术语表) |
| `## Per-Chapter Key Claims` | **一句话**说明本章确立了什么,供后续章节引用而不必重读全文 |
| `## Unresolved Cross-Chapter Issues` | 只写**后续章节**必须解决或协调的项,本章局部问题不写 |

**不写**:贡献与新颖性声明——那些在 `idea.md` 里,每章都直接读;抄一份在这里必然漂移。
整个文件要**保持精简**:它会被注入后续每一章的提示词,每一行都在后面每次运行里花钱。

**Python 后置校验**(两条都过才替换目标文件):

1. **本章留下了交接标记** —— 三个 `## ` 标题各恰好出现一次且顺序正确,且
   `## Per-Chapter Key Claims` **小节内**有本章的标记。只看那个小节的切片:文件顶部
   有一份章节顺序清单,每个文件夹名本来就在里面,全文搜的话 Stage 5 完全没跑也会判成成功。
2. **别的章一行都没少** —— 候选文件里,原文件除本章条目之外的每一行都必须还在。
   判据是"逐行还在"而不是"行数没变少":Review 被要求写出完整文件,它完全可能只留下本章,
   而前几章积累的术语约定是后续每一章对齐的唯一依据。空行差异忽略(那是无害的格式变化)。

任一条不过 → 本章判为未完成,`cross-chapter-state.md` **保持原样**,候选文件留在
`stage5-candidate.md` 供你查。`--all` 因此会停下,而不是让后面每一章在缺失术语约定的
前提下各写一套(前一章的 `final.md` 已经落盘,只看 final.md 的话重跑会永远跳过它)。

**逐章模式跳过整个 Stage 5**:那一章按自包含写的,把它的术语写进跨章状态,
会让后来 `--init` 的整篇运行继承一份不属于该结构的约定。

---

## 编译与门禁(latex/build.py,不经 Agent)

```
bibliography.md ─convert_bib→ references.bib
章节 final.md   ─convert→ sections/*.tex
                        │
                        ▼
   引用闭合门禁(前) → pdflatex/bibtex → 带日志再解析(后)
                        │
                        ▼
                  main.pdf
```

`build.py` 在编译前查引用闭合(`\cite` key 是否都在 bib),编译后扫 log 的 undefined,
有问题非零退出。

---

## 文件产出速查

| 文件 | 产出者 | 读它的人 | 用途 |
|------|--------|---------|------|
| `idea.md`(项目根) | **人工** ⭐ | Python → context-pack | 贡献/原理/方法设计。idea 类章节的主输入 |
| `outline.md`(项目根) | **人工**骨架 + `--expand` 补小节 ⭐ | Python(`--init`、路由判定) | 整篇结构。**不进任何 Agent 的上下文** |
| `outline.expanded.md` | Manager(`--expand`) | **人**(审阅后改名) | 小节与起草要点。不被 `--init` 读 |
| `data/`(项目根) | **人工** | Python → context-pack | 数字的唯一事实边界 |
| `brief.md` | Python(`--init`)或人工 | 所有 Agent | 首行 outline 指纹 + `type:` + 小节规格 |
| `input.md` | 人工(骨架由 `--init` 生成) | 所有 Agent | 本章素材,**永不被覆盖** |
| `cross-chapter-state.md` | `--init` 建骨架 + Stage 5 追加 | 后续章节的 planner/drafter | 跨章术语与结论。**仅整篇模式** |
| `context-pack.md` | Python(路由 + 内容源) | Planner | 带路由指纹、按类型排序的证据 |
| `evidence-pack.md` | Draft (Stage 0) | **Planner** 读全文;Draft 按需读 | 多视角硬证据 |
| `draft-v1.plan.md` | Manager (Stage 1a) | Draft | 施工图 + 术语表 |
| `draft-v1.part-N.md` | Draft (Stage 1b~) | Python | 分段草稿(段数自适应) |
| `draft-v1.md` | Python 拼接 | Review | 初稿 |
| `number-check.md` | Python(门禁) | Review | draft-v1 数字对账 |
| `review-v1.md` | Review (Stage 2) | Draft | 人类可读评审 |
| `review-v1.json` | Review (Stage 2) | 编排器 | 冻结验收单 + needs_citation |
| `citation-insertions.md` | Python | 人 | 补全报告:插了哪些 / 留给人哪些 |
| `draft-v2.round-N.md` | Draft (Stage 3) | Review | 每轮修订 |
| `review-verify.round-N.json` | Review (Stage 3) | 编排器 | 每轮核验 |
| `draft-v2.md` | 晋升 | Review | 修订稿 |
| `final.md` | Review (Stage 4) | 人 / LaTeX / Stage 5 | 英文定稿 |
| `final.zh.md` | Review (Stage 4) | 人 | 中文审阅版 |
| `number-check-final.md` | Python(门禁) | 人 | final 数字对账(mismatch 写进 todo) |
| `decision.md` | Draft / Review | 人 | 取舍记录 |
| `todo.md` | 每阶段 | 下一阶段 / 人 | 遗留问题 |

---

## 上下文分层原则(谁读什么)

为避免上下文爆满,机制文档**不塞给所有 Agent**,而是"喂给特定消费方":

```
idea.md + data/            → Python 装成 context-pack(两者都不直接进任何 Agent 上下文)
outline.md                 → Python 判路由 + 生成 brief(整篇目录不进任何 Agent 上下文)
邻章结构摘要(三章骨架)     → 只喂 planner(它要划清边界),且不含要点正文
context-pack(排序后证据)   → 只喂 planner(它做规划需要全景)
evidence-pack(多视角证据)  → planner 读全文并浓缩进 plan;draft 按需 read_file
plan.md(浓缩计划)          → 喂 draft(只读本段需要的信息)
证据路由子句(一段话)       → 喂 draft(每段一份,按本段小节算)和 review(判据)
写作契约子句(一段话)       → 喂每一个会改写正文的阶段 + review
cross-chapter-state         → 仅 FULL:喂 planner/drafter;SINGLE 明确禁止读它
number-check(数字对账)      → 喂 review(评审时当 MUST FIX)
```

- **planner 是 evidence-pack 与 context-pack 的唯一全量消费者**。
- **draft 不读 evidence-pack 全文**,需要细节时用 `read_file` 按需读本段相关 Q&A。
- **整篇 outline 不进任何 Agent 的上下文**。规划者只看三章骨架(前一章、本章、后一章的
  标题与小节清单),这就是"有界"的意思。
- **新增机制文档只指向它的消费方**,不让所有 Agent 的上下文变成全部文件的合集。

### Skill 机制(写作规范按需调用)

写作/图表/公式/审稿规范存成**独立 skill 文件**(`skills/`),不内联进提示词。
Agent 通过 `read_file` 按需加载:

| skill 文件 | 谁按需调用 | 何时调用 |
|---|---|---|
| `writing-style.md` | Draft | 起草正文(段落/句式/时态) |
| `figure-table-placeholder.md` | Draft | 有图/表时(写占位符不生成图) |
| `math-formula.md` | Draft | 有公式时(符号定义/LaTeX 语法) |
| `review-rubric.md` | Review | 评审时(打分/mismatch/防谄媚) |
| `experiment-writing.md` | Draft | 起草实验论文(数字纪律/防幻觉) |

> 图表占位符原则:Agent **不生成图**,只写 `[FIGURE N: 标题] + Note + 数据来源 + 支撑论断`,
> 图由人工画。表可以用结果库真实数字。

---

## 工作流里的每份文档

按"谁写的"分四类。**你只需要写第一类**;后三类是产物,看懂它们是为了知道流水线在哪一步
用了什么依据。

### 一、你写的(框架绝不替你编)

| 文档 | 一句话职责 | 不写的后果 |
|---|---|---|
| `idea.md` | 贡献是什么:一句话贡献、核心洞察、方法设计、与前人的 delta、贡献清单 | idea 类章节(方法/引言/摘要)**拒绝起草**;`--expand` 也拒绝跑 |
| `outline.md` | 结构是什么:分几章、每章 `type:`、什么顺序;小节与字数 | `--init` 没有输入,整篇模式跑不起来 |
| `data/` | 数字是什么:实验结果(CSV / JSON / 日志 / 图) | data 类章节(实验/结果/分析/消融)**拒绝起草** |
| 各章 `input.md` | 本章的补充素材:这一章特有的、不属于全局 idea 的细节 | 可以留空,流水线照跑;它**永不被覆盖** |
| `references/bibliography.md` | 可引文献库。`\cite{key}` 的 key 必须来自这里 | 引用补全匹配不到 key,全部标 needs_human 交人工 |

模板:`idea.example.md`、`outline.example.md`、`data/README.md`(数据契约)、
`workspace/_TEMPLATE/brief.md`(手建单章时照它写)。

### 二、Manager 写给你审的(唯一需要你介入的中间产物)

| 文档 | 一句话职责 |
|---|---|
| `outline.expanded.md` | `--expand` 的产物:每章的小节拆分 + 起草要点。**审阅、补字数、改名成 `outline.md`** 才生效 |

它不被 `--init` 读 —— 那道人工审阅就是这份文件存在的理由。改名前它一直是"待审"状态,
所以下次 `--expand` 见到它会拒绝跑:那说明上一份还没采纳,直接覆盖会冲掉你的审阅结果。

### 三、Python 生成的(不要手改)

| 文档 | 一句话职责 |
|---|---|
| 各章 `brief.md` | `outline.md` 在本章的投影:`type:` + 小节规格 + 字数。所有 Agent 都读它。首行盖 outline 指纹,改了 outline 没重新 `--init` 会硬停 |
| `workspace/cross-chapter-state.md` | 整篇模式的跨章记忆:章节顺序 + 各章冻结的术语符号 + 一句话结论。**仅整篇模式**,逐章模式明确禁止读 |
| 各章 `stage5-candidate.md` | Stage 5 的候选跨章状态。校验通过就原子替换上面那份并删掉自己;没通过会留下供你查 |
| 各章 `context-pack.md` | 按章节类型排好序的证据包(`idea.md` 摘录 + `data/` 摘录)。首行盖路由指纹,只喂给规划者 |
| 各章 `number-check.md` / `number-check-final.md` | 数字对账:正文里的数能不能对回 `data/`。按 gate 级别决定是拦还是只提示 |
| 各章 `citation-insertions.md` | 引用补全报告:哪些句子自动插了 `\cite`、哪些匹配不到留给你 |
| `latex/sections/*.tex` + `latex/references.bib` | `build.py` 从各章 `final.md` 与 bibliography 转换出来的编译输入 |

`brief.md` 是"手改会出事"的典型:它首行的指纹算的是 `outline.md`,你改了 brief 而 outline 没动,
指纹照样对得上,流水线不会提醒你。要改结构就改 `outline.md` 然后 `--init --force`。

### 四、Agent 写的(章节产物,按流水线顺序)

| 文档 | 写它的 Agent | 一句话职责 |
|---|---|---|
| `evidence-pack.md` | Draft (Stage 0) | 多视角硬证据问答。规划者读全文,起草者按需读 |
| `draft-v1.plan.md` | Manager (Stage 1a) | 施工图:分几段、每段覆盖哪些小节、冻结的术语表 |
| `draft-v1.part-N.md` → `draft-v1.md` | Draft (Stage 1b~) → Python 拼接 | 分段草稿与初稿 |
| `review-v1.md` / `review-v1.json` | Review (Stage 2) | 人类可读评审 / 冻结的验收单(首轮 must_fix 之后不再扩项) |
| `draft-v2.round-N.md` + `review-verify.round-N.json` | Draft ↔ Review (Stage 3) | 收敛循环每轮的修订与核验 |
| `final.md` / `final.zh.md` | Review (Stage 4) | 英文定稿 / 中文审阅版 |
| `decision.md` | Draft / Review | 取舍记录:为什么这么写、放弃了什么 |
| `todo.md` | 每阶段追加 | 遗留问题,交下一阶段或交你 |

### 五、机制文档(给 Agent 按需读,不是给你填的)

`skills/` 下五份写作与审稿规范。存成独立文件而不内联进提示词,是为了让上下文按需加载
而不是每次全量塞进去。谁在什么时候读哪一份,见上文「Skill 机制」。

### 项目自身的说明文档(不进任何流水线)

| 文档 | 内容 |
|---|---|
| `README.md` | 项目总览与快速开始 |
| `启动说明.md` | 从零到跑通的操作步骤 |
| `工作流总览.md` | 本文档:每阶段读什么、产出什么、谁执行 |
| `文档说明.md` | 文件清单与交付状态 |
| `TODO.md` | 待办与前置条件清单 |

---

## 防幻觉规则(核心)

1. **贡献的唯一来源是 `idea.md`**。框架**绝不替你编造贡献或机制**。设计细节缺失标
   `[DESIGN DETAIL NEEDED]`;`idea.md` 缺失**或还是未填的模板**→ idea 类章节直接中止。
2. **数字的唯一来源是 `data/` 结果库**。正文任何数字必须能从 context-pack 追溯到结果文件。
3. **结构的唯一来源是 `outline.md`**(整篇)或你手写的 `brief.md`(逐章)。
   Agent 不自行增删章节,也不自行改小节字数。`--expand` 也不例外:它只能往章节里补小节,
   改动章数/标题/`type:` 会被 `validate_expansion` 拒绝采纳。
4. **缺数据标 `[MISSING DATA]` / `[UNKNOWN]`,绝不猜**。不臆想硬件、超参、数据集大小。
5. **门禁 fail-closed 但分级**:data 类章节结果库为空 → 阻止起草;idea 类章节结果库为空 →
   继续但标 UNVERIFIED(方法章可以先于实验写);缺 `idea.md` → idea 类章节阻止起草。
6. **Results 与 Discussion 必须引同一组数**。
7. **证据挖掘答不出就写 "I cannot answer from the provided evidence"**,不留空档硬编。
8. **引用补全只用你提供的 bibliography**:review 标记缺引用句子 → Python 从本地 bib 匹配
   key 插 `\cite`;匹配不到标 needs_human 交人工,**绝不编造引用**。
   `search_literature` 的网页命中标为"线索,不可引"——它们没有本地 bib 条目,
   插 `\cite` 会造成悬空引用。
9. **不许跨类型代偿**:方法章不许用实验结果代替机制解释,结果章不许用方法阐述填充篇幅。
10. **不许跨模式代偿**:逐章模式不许写指向不存在章节的过渡句;整篇模式不许重复定义
    前章已固定的符号。两条都由审稿判据把守。

---

## 质量门(定稿前必须满足)

- [ ] 回答了 brief 的问题
- [ ] 贡献表述与 `idea.md` 一致,没有超出「贡献清单」的声称
- [ ] 每个数字都能对回 `data/` 结果库(数字门禁 PASS,或按 gate 级别豁免)
- [ ] 每个引用都有 bib 条目、无悬空(引用门禁 PASS)
- [ ] 没有无依据的事实、数据、机制或引用
- [ ] 没有 `[IDEA NEEDED]` / `[DESIGN DETAIL NEEDED]` / `[MISSING DATA]` 残留
- [ ] 术语、缩写、时态、叙述口径一致
- [ ] 整篇模式:符号沿用跨章状态,开合过渡到位,与邻章无重复
- [ ] 逐章模式:自包含,无指向其他章节的悬空引用
- [ ] AI 生成的文本已改写得自然
- [ ] 收敛循环内首轮 MUST FIX 已全部解决或已升级交人工
- [ ] 整篇模式:Stage 5 已把本章术语与结论写进跨章状态
