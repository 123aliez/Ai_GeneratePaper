# 实验论文多 Agent 写作 — 工作流

> 整套流水线每一步干什么、读什么、产出什么、谁干。
> 每份文档是什么(大白话)见 `文档说明.md`,从零跑起来见 `启动说明.md`。

---

## 三份你写的文件,各管一件事

框架靠三份你写的文件跑起来,职责分开、互不重复:

| 文件 | 你管什么 | 干嘛用 |
|---|---|---|
| `idea.md` | **贡献是什么**:一句话贡献、核心洞察、方法怎么设计、跟前人差在哪 | 每个阶段 Agent 的**最高优先输入**,直接读全文 |
| `outline.md` | **结构是什么**:整篇分几章、每章什么类型、什么顺序、各节多少字 | 生成每章的规格 |
| `data/` | **数字是什么**:实验结果(CSV/JSON/日志/图) | 框架据此生成 `data-index.md`,供 data 类章节导航 |

各自是**唯一来源**:贡献只从 `idea.md` 取,结构只从 `outline.md` 取,数字只从 `data/` 取。
框架不替你编任何一项——缺了就明确报错或标记,不会"合理地"瞎编一个。

`idea.md` 是全局一份、所有章节共读——不复制进任何聚合文件,而是每个阶段的提示词
第一行直接指向它、Agent 用 `read_file` 读原文。这样创新点的表述不会前后章漂移,也不会
在数据类章节被降级成"背景"。`brief.md` 不是第四份输入,它是 `outline.md` 生出来的
(每章一份),见下文。

---

## outline 两段式:骨架你写,细节框架补

`outline.md` 里混着两类信息,该由不同的人定:

| 信息 | 谁定 | 为什么 |
|---|---|---|
| 分几章、每章叫什么、什么类型、什么顺序 | **你** | 这是你对自己工作的判断,框架没有默认章节模板 |
| 每章拆哪几个小节、每节写什么、边界在哪 | **框架** | 从 `idea.md` 推出来的施工细节,正是规划该干的活 |
| 每节多少字 | **你**(可省) | 篇幅是投稿约束。你没标的,框架按该节 type 给个经验默认值,重要的你手动覆盖 |

`--expand` 会给 Manager 一套篇幅结构经验(优先保 Abstract/Intro/图1;Method 和 Experiments
是核心;理论论文 Method/Theory 更长、纯实验反之;ACL 系要独立 Limitations 等)用来切小节,
但**不替你定字数**。各 type 的经验默认词数(顶会经验值,非硬规定):

| type | 默认词数 | type | 默认词数 |
|---|---:|---|---:|
| abstract | 200 | experiments | 1800 |
| intro | 700 | results | 1800 |
| related / background | 700 | analysis / ablation | 1200 |
| method / theory | 1500 | discussion | 600 |
|  |  | limitations | 200 |
|  |  | conclusion | 300 |

没标 `(~N words)` 的小节按这张表给默认(小节级 `- type:` 优先于章 type);查不到才退回 250。
**这只是省事用的默认,不是标准**——投稿前按页数压缩,重要的章节手动标。

所以流程是三步,中间夹一道你的审阅:

```
outline.md(填表格)  ──run.py --expand──►  outline.expanded.md  ──你审 + 补字数 + 改名──►  outline.md  ──run.py --init──►  各章工作区
   章标题 + 类型            框架读 idea.md            小节 + 起草要点
```

**跳过 `--expand` 直接 `--init` 会被拒绝。** 只有章标题和类型的 outline,生成的规格里一个
小节都没有;起草会退回单段、拿不到要点,Agent 只能照章标题硬编,而流水线还会显示成功。
这道拦截在建工作区之前、调模型之前。

### 第一步:填表格

把 `outline.example.md` 复制成 `outline.md`,只需替换一张表里的占位符:

```markdown
| # | 章标题 | type |
|---|--------|------|
| 1 | Abstract      | abstract |
| 2 | Introduction  | intro    |
| 3 | Related Work  | related  |
| 4 | 频域通道注意力 | method   |
```

加减行即可,章标题中英文都行(决定文件夹名)。`type` 从词表取值(见「章节类型」)。
各列按**表头**识别而不是按位置,所以列序可换、可加备注列;没替换的占位符行整行跳过。

### 第二步:`--expand`

```bash
python run.py --expand
```

框架读 `idea.md`,为每章写 2~4 个小节,每节 2~5 条起草要点。要点里最值钱的一条是
**边界**——说清哪部分内容属于邻章、本节不许碰(「只讲机制,任何实测数字归结果章」)。
跨章重复和范围蔓延是这套流水线最常见的失败,而起草 Agent 自己判断不出边界。

它**不许**碰你的结构。越权改结构(增删/合并/拆分/重排/改名/改类型/漏展开、动你手写的
小节)一律拒绝采纳。产物落在 `outline.expanded.md`,**不原地覆盖** `outline.md`——
框架的判断要你过一遍再采纳。

### 第三步:审阅、补字数、改名

读 `outline.expanded.md`,改掉不认同的小节和要点,在小节标题后补 `(~N words)` 定篇幅:

```markdown
### 4.1 总体框架 (~350 words)
```

不标字数的小节按上面那张 type 表给经验默认值(不用手填也能跑);重要的章节你想精确控制篇幅,
就在小节标题后补 `(~N words)`:

```markdown
### 4.1 总体框架 (~350 words)
```

显式标的优先于默认值。然后:

```bash
mv outline.expanded.md outline.md
python run.py --init
```

改名这一步你做,不由框架代劳。`outline.md` 是结构的唯一来源,而 `--expand` 的产物要你过
一遍才算数——用两个文件名区分「待审」和「已采纳」,状态一眼可见。

> 不想用 `--expand` 也行:自己把 `###` 小节和要点写进 `outline.md`,`--init` 只认这一个文件。

---

## outline.md 和 brief.md 的关系

`outline.md` 是整篇目录(骨架你写、小节框架补);`python run.py --init` 把它拆成每章一个工作区:

```
outline.md  ──run.py --init──►  workspace/04-method/brief.md   (本章规格)
(整篇结构)                       workspace/cross-chapter-state.md (术语/符号备忘录)
            ──跑该章 Stage 0──►  workspace/04-method/input.md    (Manager 从 bibliography 生成的文献素材)
```

**为什么保留 brief.md 而不让流水线直接读 outline**:

- **只看自己这一章**。Agent 只该看到自己这章的规格,不该把整篇目录塞进每次调用。
- **工作区自带规格**。`workspace/04-method/` 带着一份 brief,能看出这次依据的是什么规格,
  不必回溯 outline 的历史版本。

`--init` 默认**不覆盖**已存在的 brief.md(你可能手改过),只列出"已过期"让你确认;
`input.md` 由 Stage 0 时 Manager 生成,已存在则跳过(不覆盖你审改过的内容)。

---

## 三个 Agent 各干什么

```
┌──────────────────────────────────────────────────────────────────┐
│                    Manager(规划者)                               │
│              模型：GPT-5.5                                        │
│              职责：--expand 补小节、起草前的规划、统一定义术语      │
├──────────────────────────┬───────────────────────────────────────┤
│                          │ 手下:draft(起草)、review(评审)      │
│    ┌─────────────────────┴────────────────────────┐              │
│    ▼                                              ▼              │
│  Draft(起草)                              Review(评审)        │
│  模型：Claude Opus 4.6                         模型：GPT-5.5     │
│  职责：挖证据、写正文、改稿                    职责：评审、验改、定稿、跨章交接 │
└──────────────────────────────────────────────────────────────────┘
```

**流水线的推进由 Python 代码控制,不是 Manager 控制。** Manager 只在两处做规划:
`--expand`(流水线外的一次调用)和起草前的段规划(它要看到全景才能划清段边界)。
一步步推进、门禁、断点续跑、重试全是确定性的 Python 代码。

**工具**(五个,所有 Agent 通过读写文件通信,不直接对话):
- `read_file` / `write_file` / `list_folder`(读写文件)
- `search_references`(在本地文献索引里搜)
- `search_literature`(两层搜:本地文献库 + 可选网页;网页搜到的标"只能当线索,不能引")

---

## 章节工作区

```
idea.md                        (你写) ⭐ 创新点,所有章节共读同一份
outline.md                     (你写骨架 + --expand 补小节) 整篇结构,--init 的输入
outline.expanded.md            (--expand 产物) 待你审阅、补字数、改名成 outline.md
data/                          (你写) 实验结果
└── data-index.md              (--init 时 Manager 生成,不覆盖) data/ 的三级索引
workspace/
├── cross-chapter-state.md     (--init 建空 + 每章跑完自动追加) 术语/符号备忘录
├── 01-abstract/               (--init 生成)
│   ├── brief.md               (生成) 首行指纹 + 类型 + 小节规格
│   ├── input.md               (Stage 0 时 Manager 从文献生成) 不复述 idea、不列数字
│   └── ...                    (Agent 和 Python 的产物)
└── 04-method/
```

`idea.md` 是**全局一份**(可用 `.env` 的 `IDEA_PATH` 改位置),每个阶段提示词第一行
直接指向它,Agent 读全文。`data/` 是数字的唯一来源;`data-index.md` 是 Manager 在
`--init` 时按"实验 → 结果 → 数值"三级组织出来的导航索引,存在则跳过不覆盖,数字门禁
仍直读 `data/` 原文作 ground truth。

写方法章**不必等实验跑完**:方法章的数字门禁是"有就查、没就照写",`data/` 为空也能起草。

---

## 章节类型(决定从哪取材)

**论文的贡献是创新点和原理,实验数据只是支撑证据。** 所以有两个内容源,
由 `brief.md` 顶部一行 `type:` 分流(`outline.md` 里每章的 `type:` 会写进生成的 brief):

| 类型 | 从哪取材 | 适用 |
|---|---|---|
| `idea` 类 | **`idea.md`** | abstract, intro, related, background, method, theory |
| `data` 类 | **`data-index.md`(导航 `data/` 结果)** | experiments, results, analysis, ablation |
| `mixed` 类 | 两者并重 | discussion, limitations, conclusion |

| 数字门禁 | 行为 |
|---|---|
| 严(`blocking`) | `data/` 为空 → **拒绝开写**(这类章节的内容就是数字) |
| 宽(`advisory`) | 有结果就比对,没结果只标 UNVERIFIED、不拦 |
| 不查(`off`) | 纯论述章,不跑门禁 |

**这一套贯穿四个环节**:挖证据的角度、起草的主输入、评审的标准、门禁严不严。方法章的评审
不会索要统计显著性,结果章的评审不会索要方法阐述。

**分段取材**:一份 brief 覆盖多种类型的小节时,各起草段按**自己覆盖的小节**取材。例如
Abstract+Intro 段读 idea、Results+Conclusion 段两边都读,不是整章一刀切。

**两道硬拦**:
- idea 类章节 + 没有 `idea.md`(或 `idea.md` 还是没填的模板)→ 拒绝开写
- data 类章节 + 没有 `data/` → 拒绝开写

写错的 `type:` 会退回 mixed/宽门禁并在终端提示合法值;不写会尝试从文件夹名猜
(`05-results` → results),猜不出就 mixed/宽门禁并提示补。

---

## 流水线总览

流水线对**每一章**做同一套事:备料 → 挖证据 → 规划 → 起草 → 评审 → 收敛改稿 → 定稿,
定稿后自动把本章的术语和结论交给下一章。整篇由 `--all` 按 outline 顺序逐章跑(失败即停)。

分三层:**前置**(纯 Python,不调模型)→ **主体**(调模型,五步)→ **后置**(自动交接)。
这样 Stage 0 是主体开头、Stage 5 是后置,不会平挤在一起。

```
┌─ 前置(Python,不花 token)───────────────────────────────────────────┐
│  算路由:outline 算邻章 + type 算取材源/门禁                          │
│  开跑前门禁:idea 类没 idea.md、data 类没 data/ 直接拒(不调模型)     │
└───────────────────────────────┬─────────────────────────────────────┘
                                 ▼
┌─ 主体(调模型,逐章循环)─────────────────────────────────────────────┐
│  Stage 0  备料+挖证据  管理者/起草 → input.md(文献素材)+ evidence-pack.md │
│  Stage 1  规划+起草 规划者/起草 → plan.md + 分段草稿 → draft-v1.md   │
│  Stage 2  评审     评审者       → review-v1.md + review-v1.json      │
│  Stage 3  收敛改稿 起草↔评审    → draft-v2.md                        │
│  Stage 4  定稿     评审者       → final.md + final.zh.md             │
└───────────────────────────────┬─────────────────────────────────────┘
                                 ▼
┌─ 后置(自动)─────────────────────────────────────────────────────────┐
│  Stage 5  跨章交接 评审者 → cross-chapter-state.md(术语/结论交给下一章)│
└──────────────────────────────────────────────────────────────────────┘
```

**每步速览**(谁做 / 读什么 / 产出):

| 步骤 | 执行者 | 读什么 | 产出 |
|---|---|---|---|
| 前置 | Python | outline / brief / idea / data | (无文件产出,只算路由 + 过门禁) |
| Stage 0 备料 | 管理者 | `idea.md` + `bibliography.md` + `brief.md` | `input.md`(本章该引的文献素材) |
| Stage 0 挖证据 | 起草者 | `idea.md`(+`data-index.md` 数据章)+ `brief.md` | `evidence-pack.md`(自问自答) |
| Stage 1 规划 | 规划者 | brief + 素材 + 证据 + 跨章状态 | `draft-v1.plan.md` + 术语表 |
| Stage 1 起草 | 起草者 ×N | 规划 + 上一段结尾 + 术语表 | 分段草稿 → 拼成 `draft-v1.md` |
| Stage 2 评审 | 评审者 | 草稿 + brief + 数字对账单 | `review-v1.md` + `review-v1.json` |
| Stage 3 收敛 | 起草↔评审 | 冻结的"必须修"清单 | `draft-v2.md`(≤4 轮) |
| Stage 4 定稿 | 评审者 | 两版草稿 + 评审 | `final.md` + `final.zh.md` |
| Stage 5 交接 | 评审者 | 本章 final + 备忘录 | `cross-chapter-state.md`(更新) |

深入说明(每步的角度切换、分段取材、门禁分级)见下方「各步详细说明」。

段数**自适应**:`min(小节数, 3)`,没有小节时退回单段;Abstract 这类 1-2 节的章不会被硬凑
成三段。`--progress` 是唯一支持的模式(实验模式强制),Python 控制进度、可断点续跑、
瞬时错误重试 ×4。`--direct` / `--stream-raw` / 默认 Manager 路径是综述(survey)专用,
实验模式下被拒绝——它们会绕过内容源、数字门禁和收敛循环。**断点续跑**:每个产物"存在即跳过"。

### 运行命令

```bash
python run.py --expand               # 章节骨架 → 框架补小节与要点 → outline.expanded.md
                                     #   审阅它,补 (~N words),改名成 outline.md
python run.py --init                 # outline.md → 各章工作区 + 术语备忘录
python run.py --all --progress       # 按 outline 顺序跑全部,失败即停
python run.py 04-method --progress   # 只跑其中一章(必须在 outline 里)
python run.py --list                 # 列出各章状态
```

`--all` 要求 workspace 和 outline 完全一致:缺章(还没 `--init`)或多出磁盘残留(改标题后
改名遗留)都拒绝开始,避免只跑一个子集却报告"全部完成"。

---

## 各步详细说明(谁干 / 读什么 / 写什么)

### 前置:算路由 + 开跑前门禁 —— 执行者:Python(不调模型)

**读**:
- `outline.md`(整篇结构——算这一章是第几章、邻章是什么)
- `brief.md`(本章规格——类型声明 + 首行指纹)
- `idea.md`(是否填写)、`data/`(是否有结果)

**写**:无文件产出。

> 硬拦:idea 类没 `idea.md`(或还是没填的模板)、data 类没 `data/` → 直接拒绝开写,不调模型。
> `brief.md` 首行的 outline 指纹若与 outline 不符(改了 type 没重新 --init)→ 硬停并列出陈旧产物。

---

### Stage 0a:备料 —— 执行者:Manager(规划者)

**读**:
- `idea.md`(创新点全文——挑哪条文献服务本贡献)
- `bibliography.md`(你给的参考文献清单)
- `brief.md`(本章规格)

**写**:
- `input.md`(本章该引的文献素材:相关 REF-ID + 每条对本章的意义;不复述 idea、不列数字)

> `--init` 不再生成 input.md,改由本步(Stage 0)让 Manager 从 `bibliography.md` 生成;
> 已存在则跳过(不覆盖你审改过的内容)。

---

### Stage 0b:挖证据 —— 执行者:Draft(起草者)

**读**:
- `idea.md` 全文(所有章的最高优先输入);数据类章节另读 `data-index.md` 导航数字
- `brief.md`(本章规格)

**写**:
- `evidence-pack.md`(起草前的自问自答:从 4 个评审角度对材料提硬问题再自己答,
  答不出就写"答不出",末尾列没答上来的空缺)

> 不写论文正文,不编数字和机制。

---

### Stage 1a:规划 —— 执行者:Manager(规划者)

**读**:
- `brief.md`(本章规格)、`input.md`(本章补充素材)
- `idea.md` 全文(贡献的唯一来源);数据段另读 `data-index.md`
- `evidence-pack.md`(自问自答,读全文)
- `cross-chapter-state.md`(前几章定下的术语/符号备忘录,本章直接沿用)

**写**:
- `draft-v1.plan.md`(施工图:分几段、每段覆盖哪些小节、每段的论点和禁区;
  开头一张**术语表**,全章术语/缩写/符号统一定义)
- `todo.md`(规划时已知的缺口)

---

### Stage 1b~1c:分段起草 —— 执行者:Draft(起草者)× N 段

**读**:
- `brief.md`(本章规格)、`input.md`(素材)、`draft-v1.plan.md`(施工图)
- `draft-v1.plan.md` 里的术语表(必须逐字遵守)
- `idea.md` 全文;数据段另读 `data-index.md`(每个数字必须能在里面找到)
- `evidence-pack.md`(只读本段相关的问答)
- 上一段的结尾(衔接用)

**写**:
- `draft-v1.part-1.md` … `draft-v1.part-N.md`(分段草稿,每段写自己覆盖的小节)
- 然后 Python 把它们拼成 `draft-v1.md`(初稿)

---

### 数字门禁(Stage 1 后)—— 执行者:Python(不调模型,直读 data/)

**读**:
- `draft-v1.md`(初稿)、`data/`(真实结果,直读原文作 ground truth)

**写**:
- `number-check.md`(数字对账单:正文里每个数能不能对回 `data/`,标 PASS 或逐条对不上)

> 只在 gate ≠ off 时生成(纯论述章不生成)。

---

### Stage 2:评审 —— 执行者:Review(评审者)

**读**:
- `draft-v1.md`(初稿)、`brief.md`(本章规格)、`input.md`(素材)
- `idea.md` 全文(核对贡献有没有被改写);数据章另读 `data-index.md`、`number-check.md`(数字对账单)

**写**:
- `review-v1.md`(人能读的评审:哪里好哪里差、怎么改)
- `review-v1.json`(同一次评审的机器版:打分 + "**必须修**"清单 + "这几句缺引用"清单)
- `todo.md`

---

### 引用补全(Stage 2 之后)—— 执行者:Python(不调模型)

**读**:
- `review-v1.json`(其中的"缺引用"清单:哪些句子缺引用、建议的文献 key)

**写**:
- `citation-insertions.md`(报告:哪些句自动插了 `\cite`、哪些匹配不到留给你)
- 直接在 `draft-v1.md` 对应句末尾插 `\cite{...}`

> 匹配不到 key 的标给你手动处理,绝不编造引用。

---

### Stage 3:收敛改稿 —— 执行者:Draft(起草)↔ Review(评审),最多 4 轮

**读**(每一轮):
- `review-v1.json` 里冻结的"**必须修**"清单(只认这张单)
- 上一版草稿、`brief.md`(本章规格)、`input.md`(素材)
- `idea.md` 全文;数据段另读 `data-index.md`

**写**:
- `draft-v2.round-N.md`(每一轮改出的稿)、`review-verify.round-N.json`(每一轮的核对结果)
- 全部改清后升级成 `draft-v2.md`(修订稿)
- 没改清的写进 `todo.md`、`decision.md`(取舍记录)

> 评审者只核对清单解决没、不新增问题;4 轮没清就停,剩下的交人工。

---

### Stage 4:定稿 —— 执行者:Review(评审者)

**读**:
- `draft-v1.md`(初稿)、`draft-v2.md`(修订稿)、`review-v1.md`(评审)
- `brief.md`(本章规格)、`input.md`(素材)、`idea.md` 全文、`todo.md`

**写**:
- `final.md`(英文定稿)、`final.zh.md`(中文审阅版)
- `decision.md`(取舍记录)、`todo.md`

> 定稿后 Python 再跑一次数字门禁,**覆盖** number-check.md(两次门禁只留一个文件,
> 留的是最终对账单);防改稿/合并引入错数字。

---

### Stage 5:跨章交接 —— 执行者:Review(评审者)+ Python 校验

**读**:
- 本章 `final.md`(定稿)、`todo.md`、现有的 `cross-chapter-state.md`(前几章的术语/结论)

**写**:
- `cross-chapter-draft.md`(更新后的完整备忘录——先写这个草稿版)
- Python 检查"前几章的条目都还在 + 本章也加了"通过后,用它替换 `cross-chapter-state.md`
  (检查没过就保留原文件、`--all` 停下,不污染前几章的术语)

> `cross-chapter-state.md` 按三个小节更新:本章的**术语定义**、**一句话结论**(供下一章引用)、
> **留给后续章节协调的问题**。每条以 `- [章节名]` 开头,重跑不会重复添加。

---

## 编译成 PDF(latex/build.py,不经 Agent)

```
bibliography.md ─转→ references.bib
章节 final.md   ─转→ sections/*.tex
                        │
                        ▼
   编译前查引用闭合 → pdflatex/bibtex → 编译后扫日志找 undefined
                        │
                        ▼
                  main.pdf
```

`build.py` 编译前查 `\cite` 的 key 是不是都在参考文献里,编译后扫日志找没定义的引用,
有问题就非零退出。

---

## 防幻觉规则(核心)

1. **贡献的唯一来源是 `idea.md`**。框架**绝不替你编贡献或机制**。设计细节缺失标
   `[DESIGN DETAIL NEEDED]`;`idea.md` 缺失**或还是没填的模板**→ idea 类章节直接中止。
2. **数字的唯一来源是 `data/` 结果**。正文任何数字必须能从资料追回结果文件。
3. **结构的唯一来源是 `outline.md`**。Agent 不自己增删章节,也不自己改小节字数。`--expand`
   也不例外:它只能往章节里补小节,改章数/标题/类型会被拒绝采纳。
4. **缺数据标 `[MISSING DATA]` / `[UNKNOWN]`,绝不猜**。不臆想硬件、超参、数据集大小。
5. **门禁硬拦但分级**:data 类没结果 → 阻止开写;idea 类没结果 → 继续但标 UNVERIFIED
   (方法章可以先于实验写);没 `idea.md` → idea 类章节阻止开写。
6. **Results 和 Discussion 必须引同一组数**。
7. **挖证据答不出就写 "I cannot answer from the provided evidence"**,不留空档硬编。
8. **引用补全只用你给的参考文献**:评审标记缺引用句 → Python 从本地文献匹配 key 插 `\cite`;
   匹配不到标 needs_human 交人工,**绝不编造引用**。`search_literature` 的网页命中标
   "只能当线索,不能引"——它们没有本地文献条目,插 `\cite` 会造成悬空引用。
9. **不许跨类型代偿**:方法章不许用实验结果代替机制解释,结果章不许用方法阐述填篇幅。
