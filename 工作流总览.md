# 实验论文多 Agent 写作 — 完整工作流

> 这是 `paper_agent` 框架的完整工作流总览。从 `survey/` 的综述引擎复制改造而来,核心差异:**内容源是「创新点文档 + 实验结果库」而非文献笔记库**,并新增了类型路由、证据挖掘、确定性门禁、收敛循环等机制。
>
> 配套文档:`README.md`(项目总览)、`MANIFEST.md`(文件清单)、`data/README.md`(数据契约)、`idea.example.md`(创新点文档模板)。

---

## 系统架构

```
┌──────────────────────────────────────────────────────────────────┐
│                    Manager Agent (CodeAgent)                       │
│              模型：GPT-5.5 (reasoning_effort=high)                 │
│              职责：Stage-1 规划、编排子 Agent、生成术语表           │
├──────────────────────────┬───────────────────────────────────────┤
│                          │ managed_agents=[draft_agent, review_agent]
│    ┌─────────────────────┴────────────────┐                      │
│    ▼                                      ▼                      │
│  Draft Agent (CodeAgent)            Review Agent (CodeAgent)      │
│  模型：Claude Opus 4.6                 模型：GPT-5.5              │
│  reasoning_effort=max                  reasoning_effort=high      │
│  职责：证据挖掘、起草、修订             职责：评审、VERIFY、定稿      │
└──────────────────────────────────────────────────────────────────┘
```

**工具**(四个共享,所有 Agent 通过文件系统通信,不直接对话):
- `read_file` / `write_file` / `list_folder`(文件 I/O,硬拦 index.md 全表)
- `search_references`(有界检索,上限 20 行)

**内容源(由 `config.py` 的 `PAPER_MODE` 决定)**:
- `experiment`(默认):`idea.md` 创新点文档 + `data/` 实验结果库(CSV/JSON/日志/图)
- `survey`:参考文献笔记库(与综述引擎对齐,供复用/测试)

---

## 章节类型路由(实验模式的核心机制)

**论文的贡献是创新点和原理,实验数据只是支撑证据。** 所以实验模式有两个内容源,由 `brief.md`
顶部一行 `type:` 分流:

```
brief.md 的 type:  ──►  agents/chapter_type.py  ──►  (family, gate)
                                                       │       │
                             取材源 ◄──────────────────┘       └──► 数字门禁级别
```

| family | 主输入 | 结果表的角色 | 适用类型 |
|---|---|---|---|
| `idea` | **`idea.md`** | 辅助证据,最多引一个 headline 数 | abstract, intro, related, background, method, theory |
| `data` | **`data/` 结果库** | 就是内容本身 | experiments, results, analysis, ablation |
| `mixed` | 两者并重 | 支撑论点 | discussion, limitations, conclusion |

| gate | 行为 | 适用 |
|---|---|---|
| `blocking` | `data/` 为空 → **拒绝起草**(这类章节的内容就是数字) | data 类 |
| `advisory` | 有结果就比对,没结果只提示 UNVERIFIED,不拦 | idea / mixed 类 |
| `off` | 不跑门禁(纯论述章) | related, background |

**路由贯穿四个环节**——证据挖掘视角、起草主输入、审稿判据、门禁严格度。方法章的审稿不会索要
统计显著性,结果章的审稿不会索要方法阐述。

**分段路由**:一份 brief 写整篇论文时,三个起草段各自按**自己覆盖的小节**路由。例如
Abstract+Intro 段走 idea、Results+Conclusion 段走 mixed,不是整章一刀切。

**两处 fail-closed**:
- idea 类章节 + 无 `idea.md` → 拒绝起草。创新点只有作者能给,框架不替你编
- data 类章节 + 无 `data/` → 拒绝起草。没有数据的实验章无法成立

写错的 `type:` 退回 mixed/advisory 并在终端提示合法值;不写会尝试从文件夹名推断
(`05-results` → results),仍推不出就 mixed/advisory 并提示补。

---

## 章节工作区与写作顺序

一次处理一个 workspace 文件夹(即一篇论文或一章)。文件夹里先放人工提供的 `brief.md`
(章节规格,**顶部必须声明 `type:`**)和 `input.md`(素材)。

```
idea.md                   (人工) ⭐ 全局创新点文档，所有章节共读
data/                     (人工) 实验结果库
workspace/
├── my-paper/
│   ├── brief.md          (人工) type: + 章节规格：标题 + 目标字数 + 每节要点
│   ├── input.md          (人工) 素材
│   └── ...               (Agent 产物)
└── _TEMPLATE/brief.md    (模板) 复制成你论文的 brief
```

`idea.md` 是**全局一份**(项目根,可用 `.env` 的 `IDEA_PATH` 改位置),所有章节都读同一份——
这样创新点的表述不会前后章漂移。`data/` 结果库是数字的唯一事实边界。

写方法章**不必等实验跑完**:`type: method` 的门禁是 advisory,`data/` 为空也能起草。

---

## 六步流水线总览

```
Stage 0   Stage 1a      Stage 1b      拼接     Stage 2       Stage 3          Stage 4
证据挖掘   规划         分段起草        (Python)  评审        收敛修订          定稿
Draft     Manager      Draft ×3                 Review      Draft↔Review      Review
```

`--progress` 是推荐模式(Python 控制进度、可断点续跑、瞬时错误重试 ×4)。

---

## 各阶段详细说明(读什么 / 输出什么)

### 类型路由 + 内容源装配(Stage 0 之前,Python)

**执行者**:Python(chapter_type.py + content_source.py,不耗 token)
**输入**:`brief.md` 的 `type:` 声明 + 各小节的 `- type:` 覆盖
**工作流**:
1. 解析类型 → (family, gate),小节级取并集(有 data 小节则整体 blocking)
2. 按 family 装配 `context-pack.md`:idea 章把 `idea.md` 排最前标 PRIMARY、结果表标
   SUPPORTING;data 章反过来;mixed 章两者并重
3. **两处 pre-flight fail-closed**:idea 类缺 `idea.md`、data 类缺 `data/` → 直接拒绝起草,
   **不调模型**。两个主输入都在这里查完,不等跑到后面的门禁才发现(那要白烧 4 次调用)
**输出**:`context-pack.md`(按类型排序的证据包)
**终端**:第一行 `[Manager] route |` 打印解析结果,和预期不符就改 `brief.md`,不改代码

---

### Stage 0:证据挖掘(Experiment 模式才有)

**执行者**:Draft Agent
**输入**:
- `context-pack.md`(按类型排序:idea 章是创新点文档,data 章是结果库)
- brief.md(本节要回答什么)
**工作流**:
1. 按 4 个审稿视角各提 3-5 个硬问题。**视角随 family 切换**:
   - idea 章:新颖性 / 机制(为什么work) / 精确性(能否复现实现) / 替代设计
   - data 章:统计显著性 / baseline 与消融 / 可复现性 / 过度声称
   - mixed 章:论断-证据对应 / 机制是否被证实 / 过度声称 / 局限
2. 逐问作答。**grounding 规则也随 family 变**:idea 章必须引用作者原话,
   data 章必须引用具体指标值。答不出就写 "I cannot answer from the provided evidence"
3. 把问答写成证据包
**输出**:`evidence-pack.md`(多视角 Q&A + 末尾 Open Gaps 清单)
**不做**:不写论文正文;不编数字,不编机制。

---

### Stage 1a:规划(Manager/Planner)

**执行者**:Manager Agent(无 Manager 时 Draft)
**输入**:
- brief.md(本章任务)
- input.md(素材)
- context-pack.md(实验模式:按类型排序的证据)或 cross-chapter-state.md(综述模式)
- evidence-pack.md(上一阶段的证据包)
- 类型路由子句(告诉 planner 哪个源是主输入)
- filtered reference excerpt(有界文献)
**工作流**:
1. 读 brief 明确本节必须回答的问题
2. 读 input + context-pack 确定证据边界
3. 读 evidence-pack 确定有哪些可用的硬证据
4. 写规划,开头固定一个 `## Notation and Terminology Table`(全章术语/缩写/符号冻结)
5. 分三段,每段指定:target claims、required REF IDs、禁区、过渡作用、输出文件
**输出**:
- `draft-v1.plan.md`(含术语表)
- `todo.md`(规划已知缺口)

---

### Stage 1b:分段起草(Draft ×3)

**执行者**:Draft Agent(分三次调用)
**输入**:
- brief.md / input.md / draft-v1.plan.md
- context-pack.md(按类型排序的证据源)
- **该段自己的类型路由子句**(按这段覆盖的小节算,不是整章一刀切)
- evidence-pack.md(硬证据)
- 上一段的结尾(tail_text,≤1200 字符,段间衔接)
- 术语表(必须遵守,禁止重定义)
**工作流**(每段一次):
1. 读计划里属于自己的那段边界
2. 读上一段结尾 + 术语表,保持衔接和术语一致
3. 只写本段,不碰其他段
4. 按本段的 family 取材:
   - idea 段:从 `## Core idea` 展开机制和设计,结果表最多引一个 headline 数;
     设计细节缺失标 [DESIGN DETAIL NEEDED],**不许"合理地"补一个**
   - data 段:只写结果表里的真实数字,缺的标 [MISSING DATA];不重述方法设计
**输出**:
- `draft-v1.part-1.md` / `draft-v1.part-2.md` / `draft-v1.part-3.md`

**拼接(Python,无模型)**:三 part 拼成 `draft-v1.md`。

---

### 数字门禁(实验模式,Stage 1 后自动,按类型分级)

**执行者**:Python(number_gate.py,确定性,不耗 token)
**输入**:`draft-v1.md` + `data/` 结果库 + 本章 gate 级别
**工作流**:逐个数与结果库对账,抓幻觉数字
**输出**:`number-check.md`(标注 gate 级别 + PASS 或逐条 MISMATCH)
**分级行为**:
- `blocking`(data 类):结果库为空的情况已在 pre-flight 拦掉,到这里说明有数据,
  MISMATCH 注入 Stage 2 当 MUST FIX
- `advisory`(idea/mixed 类):结果库为空 → 打印 UNVERIFIED 提示后继续。有数据则照常对账
- `off`(related/background):完全不跑

---

### Stage 2:评审(Review)

**执行者**:Review Agent
**输入**:
- draft-v1.md
- brief.md / input.md
- **类型判据子句**(告诉审稿这章按什么标准判)
- number-check.md(实验模式:数字门禁结果)
- evidence-pack.md(核对证据是否被正确引用)
- context-pack.md / cross-chapter-state.md
- filtered reference excerpt
**工作流**:
1. 读 draft-v1 + brief 确认是否回答了该回答的问题
2. **按类型套判据**:
   - idea 章:机制是否只是断言而未解释?设计选择有没有理由?有没有跑偏成结果复述?
     **明确不许索要更多实验数字,缺指标不算缺陷**
   - data 章:每个数字能否对回结果表?baseline/消融是否齐全?方差与种子有没有报?
     结论是否强于数据支撑?**不许索要方法阐述**
3. 读 number-check.md,把每条 MISMATCH 当 MUST FIX
4. 按 7 维打分(Accuracy/Completeness/Clarity/Structure/Readability/AI Traces/Style)
5. 问题分 MUST FIX / SHOULD FIX / CONSIDER
6. 写 Markdown 评审 + JSON 清单(JSON 是修订的验收单)

> **为什么审稿判据必须分流**:不分流的话,审稿会对方法章统一套实验论文标准,把
> "缺统计显著性""缺 baseline"塞进 MUST FIX——而这些正是收敛循环的冻结验收单,
> Draft 会花 4 轮去满足一堆方法章本来就不该报的要求。
**输出**:
- `review-v1.md`(人类可读评审)
- `review-v1.json`(机器可读:分数 + must_fix 清单 ← 收敛循环的冻结验收单,含 `needs_citation` 数组)
- `todo.md`

---

### 引用补全(Stage 2 之后,Python 执行)

**执行者**:Python(citation_supplement 的 `apply_insertions`,确定性)
**输入**:review-v1.json 的 `needs_citation`(review 标记的缺引用句子 + 建议 bib key)
**工作流**:
1. 只从用户提供的 bibliography 匹配 key,把 `\cite{REF-XXXX}` 机械插进那句末尾
2. 匹配不到 key 的 → 标 needs_human,交用户决定,绝不编造
**输出**:`citation-insertions.md`(插了哪些 / 留给人哪些)

> 分工:review 负责**判断**(标记缺引用句子 + 建议 key),Python 负责**执行**(机械插入)。review 不亲自改稿。

---

### Stage 3:收敛修订(Draft ↔ Review,最多 4 轮)

**执行者**:Draft Agent(修订)+ Review Agent(VERIFY)
**输入(每轮)**:
- 冻结的 must_fix 清单(review-v1.json,只认这张单)
- 上一版草稿 + brief / input / context-pack
**工作流**:
1. Draft 读冻结清单,逐条在对应位置修复
2. Review 进 VERIFY 模式:**只核验冻结清单是否解决,不新增问题**
3. 全解决 → 晋升为 draft-v2;未全解决 → 再修一轮
4. 到 4 轮仍没清 → 剩余项写进 todo.md / decision.md 交人工
**输出**:
- `draft-v2.round-N.md`(每轮一版)
- `draft-v2.md`(通过的那轮晋升)
- `review-verify.round-N.json`(每轮核验结果)
- `todo.md` / `decision.md`(到顶时记升级)
**不做**:不新增验收单之外的问题;不无限循环。

---

### Stage 4:定稿(Review)

**执行者**:Review Agent
**输入**:
- draft-v1.md / draft-v2.md / review-v1.md
- brief.md / input.md / context-pack.md
- todo.md(未决项)
**工作流**:
1. 合并两版最佳部分,解决所有 MUST FIX / SHOULD FIX
2. 实验模式:数字必须与结果库一致
3. 无 AI 痕迹、术语一致、引用完整
4. 定稿后 Python 再跑一次数字门禁(防修订/合并引入新错误数字)
**输出**:
- `final.md`(英文 publication-ready)
- `final.zh.md`(中文审阅版)
- `decision.md`(取舍记录)
- `todo.md`(剩余问题;final 数字门禁的 mismatch 也会追加到这里)
**不做**:不把两版拼成长冗文本;final 里不含评审意见。

---

## 编译与门禁(latex/build.py,不经 Agent)

```
bibliography.md ─convert_bib→ references.bib
章节 final.md   ─convert→ sections/*.tex
                        │
                        ▼
   引用闭合门禁(前) → pdflatex/bibtex → 带日志再解析(后)
                        │
                        ▼
                  main.pdf
```

`build.py` 在编译前查引用闭合(`\cite` key 是否都在 bib),编译后扫 log 的 undefined,有问题非零退出。

---

## 各阶段文件产出速查

| 文件 | 产出者 | 读它的人 | 用途 |
|------|--------|---------|------|
| `idea.md`(项目根) | **人工** ⭐ | Python → context-pack | 创新点/原理/方法设计。idea 类章节的主输入 |
| `brief.md` | 人工 | 所有 Agent | `type:` 声明 + 章节规格 |
| `input.md` | 人工 | 所有 Agent | 素材 |
| `context-pack.md` | Python (类型路由 + 内容源) | Planner | 按类型排序的证据:idea 章创新点在前、data 章结果表在前 |
| `evidence-pack.md` | Draft (Stage 0) | **Planner**(消化进 plan) | 多视角硬证据;draft 按需 read_file |
| `draft-v1.plan.md` | Manager (Stage 1a) | Draft | 施工图 + 术语表 |
| `draft-v1.part-N.md` | Draft (Stage 1b) | Python | 分段草稿 |
| `draft-v1.md` | Python 拼接 | Review | 初稿 |
| `number-check.md` | Python (门禁) | Review | draft-v1 数字对账 |
| `review-v1.md` | Review (Stage 2) | Draft | 人类可读评审 |
| `review-v1.json` | Review (Stage 2) | 编排器 | 冻结验收单 + needs_citation |
| `citation-insertions.md` | Python (C11) | 人 | 补全报告:插了哪些 / 留给人哪些 |
| `draft-v2.round-N.md` | Draft (Stage 3) | Review | 每轮修订 |
| `review-verify.round-N.json` | Review (Stage 3) | 编排器 | 每轮核验 |
| `draft-v2.md` | 晋升 | Review | 修订稿 |
| `final.md` | Review (Stage 4) | 人/LaTeX | 英文定稿 |
| `final.zh.md` | Review (Stage 4) | 人 | 中文审阅版 |
| `number-check-final.md` | Python (门禁) | 人 | final 数字对账(有 mismatch 写进 todo) |
| `decision.md` | Draft/Review | 人/下一章 | 取舍记录 |
| `todo.md` | 每阶段 | 下一阶段/人 | 遗留问题 |

---

## 上下文分层原则(谁读什么)

为避免上下文爆满,机制文档**不塞给所有 Agent**,而是"喂给特定消费方":

```
idea.md + data/           → Python 按类型装配成 context-pack(不直接进任何 Agent 上下文)
context-pack(排序后证据)  → 只喂 planner(它做规划需要全景)
evidence-pack(多视角证据) → 只喂 planner(消化进 plan.md)
plan.md(浓缩计划)         → 喂 draft(只读本段需要的信息)
类型路由子句(一段话)      → 喂 draft(每段一份,按本段小节算)和 review(判据)
number-check(数字对账)    → 喂 review(评审时当 MUST FIX)
```

- **planner 是 evidence-pack 的唯一全量消费者**——它把每段需要的证据浓缩进 plan.md。
- **draft 不读 evidence-pack 全文**,需要细节时用 `read_file` 按需读本段相关 Q&A。
- **新增机制文档只指向它的消费方**,不让所有 Agent 的上下文变成全部文件的合集。

### Skill 机制(写作规范按需调用)

写作/图表/公式/审稿规范存成**独立 skill 文件**(`skills/`),不内联进提示词。Agent 通过 `read_file` 按需加载:

| skill 文件 | 谁按需调用 | 何时调用 |
|---|---|---|
| `writing-style.md` | Draft | 起草正文(段落/句式/时态) |
| `figure-table-placeholder.md` | Draft | 有图/表时(写占位符不生成图) |
| `math-formula.md` | Draft | 有公式时(符号定义/LaTeX语法) |
| `review-rubric.md` | Review | 评审时(打分/mismatch/防谄媚) |
| `experiment-writing.md` | Draft | 起草实验论文(数字纪律/防幻觉) |

> 图表占位符原则:Agent **不生成图**,只写 `[FIGURE N: 标题] + Note + 数据来源 + 支撑论断`,图由人工画。表可以用结果库真实数字。

## 实验模式防幻觉规则(核心)

1. **创新点唯一来源是 `idea.md`**。框架**绝不替你编造贡献或机制**。设计细节缺失标
   `[DESIGN DETAIL NEEDED]`,创新点整体缺失标 `[IDEA NEEDED]` 并中止 idea 类章节。
2. **数字唯一来源是 `data/` 结果库**。正文任何数字必须能从 context-pack 追溯到结果文件。
3. **缺数据标 `[MISSING DATA]` / `[UNKNOWN]`,绝不猜**。不臆想硬件、超参、数据集大小。
4. **门禁 fail-closed 但分级**:data 类章节结果库为空 → 阻止起草;idea 类章节结果库为空 →
   继续但标 UNVERIFIED(方法章可以先于实验写);缺 `idea.md` → idea 类章节阻止起草。
5. **Results 与 Discussion 必须引同一组数**。
6. **证据挖掘答不出就写 "I cannot answer from the provided evidence"**,不留空档硬编。
7. **引用补全只用用户提供的 bibliography**:review 标记缺引用句子 → Python 从本地 bib 匹配 key 插 `\cite`;匹配不到标 needs_human 交人工,绝不编造引用。
8. **不许跨类型代偿**:方法章不许用实验结果代替机制解释,结果章不许用方法阐述填充篇幅。
   审稿判据按类型分流就是为了守住这条。

---

## 质量门(定稿前必须满足)

- [ ] 回答了 brief 的问题
- [ ] 创新点表述与 `idea.md` 一致,没有超出「贡献清单」的声称
- [ ] 每个数字都能对回 data/ 结果库(数字门禁 PASS,或按 gate 级别豁免)
- [ ] 每个引用都有 bib 条目、无悬空(引用门禁 PASS)
- [ ] 没有无依据的事实、数据、机制或引用
- [ ] 没有 `[IDEA NEEDED]` / `[DESIGN DETAIL NEEDED]` / `[MISSING DATA]` 残留
- [ ] 术语、缩写、时态、叙述口径一致
- [ ] AI 生成的文本已改写得自然
- [ ] 收敛循环内首轮 MUST FIX 已全部解决或已升级交人工
