# 实验论文多 Agent 写作 — 完整工作流

> 本文档描述整个系统的多 Agent 工作流程:每个阶段读什么、产出什么、谁执行。
> 文档清单见 `文档说明.md`,从零跑起来见 `启动说明.md`。

---

## 三份人工输入,各管一件事

框架跑起来靠三份你写的文件,职责严格分开,互不重复:

| 文件 | 管什么 | 谁读 |
|---|---|---|
| `idea.md` | **贡献是什么**:一句话贡献、核心洞察、方法设计、与前人的 delta | Python 装进 context-pack |
| `outline.md` | **结构是什么**:整篇分几章、每章什么 `type:`、什么顺序、各节多少字 | Python 生成各章 brief |
| `data/` | **数字是什么**:实验结果(CSV/JSON/日志/图) | Python 装进 context-pack |

各自都是**唯一事实来源**:贡献只从 `idea.md` 取,结构只从 `outline.md` 取,数字只从 `data/` 取。
框架不替你编任何一项——缺了就明确报错或标记,不"合理地"补一个。

`brief.md` 不是第四份输入。它是 `outline.md` 的**生成产物**(每章一份),见下文。

---

## outline 的两段式:骨架你写,细节 Manager 补

`outline.md` 里其实混着两类信息,该由不同的人来定:

| 信息 | 谁定 | 为什么 |
|---|---|---|
| 分几章、每章叫什么、什么 `type:`、什么顺序 | **你** | 这是你对自己工作的判断,框架没有默认章节模板 |
| 每章拆哪几个小节、每节写什么、边界在哪 | **Manager** | 从 `idea.md` 推导出来的施工细节,正是规划该干的活 |
| 每节多少字 | **你** | 篇幅是投稿约束(会议页数、章节配比),Manager 不知道 |

所以流程是三步,中间夹一道你的审阅:

```
outline.md(填表格)  ──run.py --expand──►  outline.expanded.md  ──你审 + 补字数 + 改名──►  outline.md  ──run.py --init──►  各章工作区
   章标题 + type              Manager 读 idea.md            小节 + 起草要点
```

**跳过 `--expand` 直接 `--init` 会被拒绝。** 只有章标题和 `type:` 的 outline,生成的 brief
里一个小节都没有;起草会退回单段、拿不到任何要点,Draft 只能照章标题硬编,而流水线
全程显示成功。这道拦截在建工作区之前、调模型之前。

### 第一步:填表格

`outline.example.md` 复制成 `outline.md`,只需替换一张表里的占位符:

```markdown
| # | 章标题 | type |
|---|--------|------|
| 1 | Abstract      | abstract |
| 2 | Introduction  | intro    |
| 3 | Related Work  | related  |
| 4 | 频域通道注意力 | method   |
```

加减行即可,章标题中英文都行(决定文件夹名)。`type` 从词表取值(见「章节类型路由」)。
各列按**表头**识别而不是按位置,所以列序可换、可加备注列;没替换的占位符行整行跳过。

### 第二步:`--expand`

```bash
python run.py --expand
```

Manager 读 `idea.md`,为每章写 2~4 个小节,每节 2~5 条起草要点。要点里最值钱的一条是
**边界**——说清哪部分内容属于邻章、本节不许碰(「只讲机制,任何实测数字归结果章」)。
跨章重复与范围蔓延是这套流水线最常见的失败,而起草 Agent 自己判断不出边界。

它**不许**碰你的结构。越权改结构(增删/合并/拆分/重排/改名/改 type/漏展开、动作者手写
的小节)一律拒绝采纳。产物落在 `outline.expanded.md`,**从不原地覆盖** `outline.md`:
Manager 的判断要你过一遍再采纳。

### 第三步:审阅、补字数、改名

读 `outline.expanded.md`,改掉不认同的小节与要点,在小节标题后补 `(~N words)` 定篇幅:

```markdown
### 4.1 总体框架 (~350 words)
```

不标字数的小节按默认 250 词处理,`--expand` 跑完会告诉你还有几个没标。然后:

```bash
mv outline.expanded.md outline.md
python run.py --init
```

改名这一步由你做,不由框架代劳。`outline.md` 是结构的唯一事实来源,而 `--expand`
的产物需要你过一遍才算数 —— 用两个文件名区分「待审」和「已采纳」,状态一眼可见。

> 不想用 `--expand` 也行:自己把 `###` 小节与要点写进 `outline.md`,`--init` 只认这一个文件。

---

## outline.md 与 brief.md 的关系

`outline.md` 是整篇目录(骨架你写、小节由 `--expand` 补);`python run.py --init` 把它拆成每章一个工作区:

```
outline.md  ──run.py --init──►  workspace/04-method/brief.md   (本章规格)
(整篇结构)                       workspace/04-method/input.md    (素材,待你填)
                                 workspace/cross-chapter-state.md (跨章状态)
```

**为什么保留 brief.md 而不让流水线直接读 outline**:

- **上下文分层**。Agent 只该看到自己这一章的规格,不该把整篇目录塞进每次调用。
- **工作区自带规格**。`workspace/04-method/` 带着一份 brief,能看出这次运行依据的是什么规格,
  不必回溯 outline 的历史版本。

`--init` 默认**不覆盖**已存在的 brief.md(你可能手改过),只列出"已过期"让你确认;
`input.md` 即使 `--force` 也永不覆盖——那是你的素材。

---

## 系统架构

```
┌──────────────────────────────────────────────────────────────────┐
│                    Manager Agent (CodeAgent)                       │
│              模型：GPT-5.5 (reasoning_effort=high)                 │
│              职责：outline 展开(--expand)、Stage-1a 规划、术语表    │
├──────────────────────────┬───────────────────────────────────────┤
│                          │ managed_agents=[draft_agent, review_agent]
│    ┌─────────────────────┴────────────────────────┐                      │
│    ▼                                      ▼                      │
│  Draft Agent (CodeAgent)            Review Agent (CodeAgent)      │
│  模型：Claude Opus 4.6                 模型：GPT-5.5              │
│  reasoning_effort=max                  reasoning_effort=high      │
│  职责：证据挖掘、起草、修订             职责：评审、VERIFY、定稿、Stage 5 │
└──────────────────────────────────────────────────────────────────┘
```

**编排由 Python 做,不由 Manager 做。** Manager 只承担两处规划:`--expand` 的 outline 展开
(流水线之外的一次性调用)和 Stage 1a 的段规划(它需要全景才能划清段边界)。
阶段推进、门禁、断点续跑、重试全部是 `orchestrator.py` 里的确定性代码。

**工具**(五个共享,所有 Agent 通过文件系统通信,不直接对话):
- `read_file` / `write_file` / `list_folder`(文件 I/O)
- `search_references`(有界检索本地文献索引)
- `search_literature`(两层检索:本地 bib + 可选网页 LLM;web 命中标为"线索,不可引")

---

## 章节工作区

```
idea.md                        (人工) ⭐ 全局创新点,所有章节共读同一份
outline.md                     (人工骨架 + --expand 补小节) 整篇结构,--init 的输入
outline.expanded.md            (--expand 产物) 待你审阅、补字数、改名成 outline.md
data/                          (人工) 实验结果库
workspace/
├── cross-chapter-state.md     (--init 生成 + Stage 5 追加) 跨章术语与结论载体
├── 01-abstract/               (--init 生成)
│   ├── brief.md               (生成) 首行 outline 指纹 + type: + 小节规格
│   ├── input.md               (生成骨架,你填素材) 永不被覆盖
│   └── ...                    (Agent 与 Python 的产物)
└── 04-method/
```

`idea.md` 是**全局一份**(可用 `.env` 的 `IDEA_PATH` 改位置),所有章节都读同一份——
这样创新点的表述不会前后章漂移。`data/` 结果库是数字的唯一事实边界。

写方法章**不必等实验跑完**:`type: method` 的门禁是 advisory,`data/` 为空也能起草。

---

## 章节类型路由(决定从哪儿取材)

**论文的贡献是创新点和原理,实验数据只是支撑证据。** 所以有两个内容源,
由 `brief.md` 顶部一行 `type:` 分流(`outline.md` 里每章的 `type:` 会写进生成的 brief):

```
outline.md 每章的 type:  ──►  brief.md  ──►  agents/chapter_type.py  ──►  (family, gate)
                                                                          │        │
                                                    取材源 ◄──────────────┘        └──► 数字门禁级别
```

| family | 主输入 | 适用类型 |
|---|---|---|
| `idea` | **`idea.md`** | abstract, intro, related, background, method, theory |
| `data` | **`data/` 结果库** | experiments, results, analysis, ablation |
| `mixed` | 两者并重 | discussion, limitations, conclusion |

| gate | 行为 |
|---|---|
| `blocking` | `data/` 为空 → **拒绝起草**(这类章节的内容就是数字) | 
| `advisory` | 有结果就比对,没结果只提示 UNVERIFIED,不拦 |
| `off` | 不跑门禁(纯论述章) |

**贯穿四个环节**——证据挖掘视角、起草主输入、审稿判据、门禁严格度。方法章的审稿不会索要
统计显著性,结果章的审稿不会索要方法阐述。

**分段路由**:一份 brief 覆盖多种类型的小节时,各起草段按**自己覆盖的小节**路由。例如
Abstract+Intro 段走 idea、Results+Conclusion 段走 mixed,不是整章一刀切。

**两处 fail-closed**:
- idea 类章节 + 无 `idea.md`(或 `idea.md` 还是未填的模板)→ 拒绝起草
- data 类章节 + 无 `data/` → 拒绝起草

写错的 `type:` 退回 mixed/advisory 并在终端提示合法值;不写会尝试从文件夹名推断
(`05-results` → results),仍推不出就 mixed/advisory 并提示补。

---

## 流水线总览

```
Stage 0    Stage 1a    Stage 1b~1c   拼接      Stage 2    Stage 3        Stage 4   Stage 5
证据挖掘    规划        分段起草       (Python)   评审       收敛修订        定稿      跨章交接
Draft      Manager     Draft ×N                Review     Draft↔Review   Review    Review
```

段数**自适应**:`min(小节数, 3)`,没有小节时退回单段。Abstract 这类只有 1-2 节的章不会被硬凑
成三段(那会生出没有来源的空段,并给它们不合理的字数目标)。

`--progress` 是唯一支持的模式(实验模式下强制)。Python 控制进度、可断点续跑、
瞬时错误自动重试 ×4。`--direct` / `--stream-raw` / 默认 Manager 路径是 survey 专用,
在实验模式下会被显式拒绝——它们会绕过内容源、数字门禁和收敛循环。

**断点续跑**:每个产物"存在即跳过"。

### 运行命令

```bash
python run.py --expand               # 章节骨架 → Manager 补小节与要点 → outline.expanded.md
                                     #   审阅它,补 (~N words),改名成 outline.md
python run.py --init                 # outline.md → 各章工作区 + 跨章状态
python run.py --all --progress       # 按 outline 顺序跑全部,失败即停
python run.py 04-method --progress   # 只跑其中一章(该章必须在 outline 里)
python run.py --list                 # 列出各章状态
```

`--all` 要求 workspace 与 outline 完全一致:缺章(还没 `--init`)或多出磁盘残留(改标题后
改名遗留)都会拒绝开始,避免只跑一个子集却报告"全部完成"。

---

## 各阶段详细说明(读什么 / 输出什么)

### 前置:路由解析 + 内容源装配(Stage 0 之前,Python)

**执行者**:Python(`outline.py` + `chapter_type.py` + `content_source.py`,不耗 token)

**输入**:
- `outline.md`(算邻章、算 brief 指纹)
- `brief.md` 的 `type:` 声明 + 各小节的 `- type:` 覆盖
- `idea.md` / `data/`

**工作流**:
1. **写作路由**:该章在 outline 里算第几章、邻章是什么。构造写作契约(起草侧 + 审稿侧)
   与邻章结构摘要
2. **证据路由**:解析 `type:` → (family, gate),小节级取并集(有 data 小节则整体 blocking)
3. **两处 pre-flight fail-closed**:idea 类缺 `idea.md`(或还是未填模板)、data 类缺 `data/`
   → 拒绝起草,**不调模型**
4. 按 family 装配 `context-pack.md`:idea 章把 `idea.md` 排最前标 PRIMARY、结果表标
   SUPPORTING;data 章反过来;mixed 章两者并重
5. 这一步**装配失败就停**(fail-closed),不进任何 Agent 阶段

**输出**:`context-pack.md`(按类型排序的证据包)

**终端**:`[Manager] route |` 与 `[Manager] write-mode |` 两行打印解析结果。
和预期不符就改 `outline.md` / `brief.md`,不改代码。

---

### Stage 0:证据挖掘(实验模式才有)

**执行者**:Draft Agent

**输入**:`context-pack.md`(按类型排序)+ `brief.md`

**工作流**:
1. 按 4 个审稿视角各提 3-5 个硬问题。**视角随 family 切换**:
   - idea 章:新颖性 / 机制(为什么 work)/ 精确性(能否复现实现)/ 替代设计
   - data 章:统计显著性 / baseline 与消融 / 可复现性 / 过度声称
   - mixed 章:论断-证据对应 / 机制是否被证实 / 过度声称 / 局限
2. 逐问作答。**grounding 规则也随 family 变**:idea 章必须引用作者原话,
   data 章必须引用具体指标值。答不出就写 "I cannot answer from the provided evidence"

**输出**:`evidence-pack.md`(多视角 Q&A + 末尾 Open Gaps 清单)

**不做**:不写论文正文;不编数字,不编机制。

---

### Stage 1a:规划(Manager)

**执行者**:Manager Agent(无 Manager 时退回 Draft)

**输入**:
- `brief.md`(本章规格)/ `input.md`(素材)
- `context-pack.md`(按类型排序的证据)
- `evidence-pack.md`(**读全文**——它是规划的证据基础)
- 证据路由子句(哪个源是主输入)
- **写作契约子句**(第几章/可跨章引用/复用前章符号)
- 邻章结构摘要 + `cross-chapter-state.md` 的路径
- Python 算好的段边界(不许移动小节)
- filtered reference excerpt(有界文献)

**工作流**:
1. 读 brief 明确本章必须回答的问题
2. 读 input + context-pack 确定证据边界
3. 读 evidence-pack 全文,把每段需要的证据**浓缩进计划**(起草者读计划,不读整个 pack)
4. 写规划,开头固定一个 `## Notation and Terminology Table`——全章术语/缩写/符号冻结,
   沿用跨章状态里已有的定义,不另立一套
5. 每段指定:target claims、required REF IDs、禁区(不许碰的内容)、过渡作用、输出文件

**输出**:`draft-v1.plan.md`(含术语表)、`todo.md`(规划已知缺口)

---

### Stage 1b~1c:分段起草(Draft ×N)

**执行者**:Draft Agent(每段一次调用)

**输入**:
- `brief.md` / `input.md` / `draft-v1.plan.md`
- **该段自己的证据路由子句**(按这段覆盖的小节算,不是整章一刀切)
- **写作契约子句**(同 Stage 1a)
- 上一段的结尾(≤1200 字符,保证段间衔接)
- 术语表(必须逐字遵守,禁止重定义前段已固定的符号)
- `evidence-pack.md`(**按需** `read_file` 读本段相关的 Q&A,不整段内联)

**工作流**(每段一次):
1. 读计划里属于自己的那段边界
2. 读上一段结尾 + 术语表,保持衔接和术语一致
3. 只写本段,不碰其他段
4. 按本段的 family 取材:
   - idea 段:从 `## Core idea` 展开机制和设计,结果表最多引一个 headline 数;
     设计细节缺失标 `[DESIGN DETAIL NEEDED]`,**不许"合理地"补一个**
   - data 段:只写结果表里的真实数字,缺的标 `[MISSING DATA]`;不重述方法设计

**输出**:`draft-v1.part-1.md` … `draft-v1.part-N.md`

**拼接(Python,无模型)**:按**本次实际段数**拼成 `draft-v1.md`。

---

### 数字门禁(Stage 1 后,Python 自动,按类型分级)

**执行者**:Python(`number_gate.py`,确定性,不耗 token)

**输入**:`draft-v1.md` + `data/` 结果库 + 本章 gate 级别

**工作流**:逐个数与结果库对账(邻近度绑定 + 量纲缩放容错),抓幻觉数字

**输出**:`number-check.md`(标注 gate 级别 + PASS 或逐条 MISMATCH)

---

### Stage 2:评审(Review)

**执行者**:Review Agent

**输入**:
- `draft-v1.md` / `brief.md` / `input.md` / `context-pack.md`
- **证据类型判据子句**(这章按什么标准判)
- **写作模式判据子句**(查重复定义、查跨章重复、查开合过渡缺失)
- `number-check.md`(数字门禁结果)
- filtered reference excerpt

**工作流**:
1. 读 draft-v1 + brief 确认是否回答了该回答的问题
2. **按证据类型套判据**:
   - idea 章:机制是否只是断言而未解释?设计选择有没有理由?有没有跑偏成结果复述?
     **明确不许索要更多实验数字,缺指标不算缺陷**
   - data 章:每个数字能否对回结果表?baseline/消融是否齐全?方差与种子有没有报?
     结论是否强于数据支撑?**不许索要方法阐述**
3. 读 `number-check.md`,把每条 MISMATCH 当 MUST FIX
4. 按 7 维打分(Accuracy / Completeness / Clarity / Structure / Readability /
   AI Traces / Style Consistency)
5. 问题分 MUST FIX / SHOULD FIX / CONSIDER
6. 写 Markdown 评审 + JSON 清单(JSON 是修订阶段的验收单)

**输出**:
- `review-v1.md`(人类可读评审)
- `review-v1.json`(机器可读:7 维分数 + must_fix 冻结清单 + `needs_citation` 数组)
- `todo.md`

---

### 引用补全(Stage 2 之后,Python)

**执行者**:Python(`citation_supplement.apply_insertions`,确定性)

**输入**:`review-v1.json` 的 `needs_citation`(缺引用的句子 + 建议 bib key)

**工作流**:
1. 只从**你提供的** bibliography 匹配 key,把 `\cite{REF-XXXX}` 机械插进那句末尾
2. 匹配不到 key 的 → 标 needs_human 交你决定,**绝不编造**

**输出**:`citation-insertions.md`(插了哪些 / 留给人哪些)

---

### Stage 3:收敛修订(Draft ↔ Review,最多 4 轮)

**执行者**:Draft Agent(修订)+ Review Agent(VERIFY)

**输入(每轮)**:
- 冻结的 must_fix 清单(只认这张单)
- 上一版草稿 + `brief.md` / `input.md` / `context-pack.md`
- **写作契约子句**(每一轮都带——收敛循环会重写整篇草稿)

**工作流**:
1. Draft 读冻结清单,逐条在对应位置修复,保留已正确的段落
2. Review 进 VERIFY 模式:**只核验冻结清单是否解决,不新增问题**
3. 全解决 → 晋升为 `draft-v2.md`;未全解决 → 再修一轮
4. 到 4 轮仍没清 → 剩余项写进 `todo.md` / `decision.md` 交人工,不空转

**输出**:`draft-v2.round-N.md`(每轮)、`review-verify.round-N.json`(每轮核验)、
`draft-v2.md`(通过的那轮晋升)、`todo.md` / `decision.md`

---

### Stage 4:定稿(Review)

**执行者**:Review Agent

**输入**:`draft-v1.md` / `draft-v2.md` / `review-v1.md` / `brief.md` / `input.md` /
`context-pack.md` / `todo.md` + **写作契约子句**

**工作流**:
1. 合并两版最佳部分,解决所有 MUST FIX / SHOULD FIX
2. 数字必须与结果库一致
3. 无 AI 痕迹、术语一致、引用完整
4. 定稿后 Python 再跑一次数字门禁(防修订/合并引入新的错误数字)

**输出**:`final.md`(英文 publication-ready)、`final.zh.md`(中文审阅版)、
`decision.md`(取舍记录)、`todo.md`

**不做**:不把两版拼成长冗文本;final 里不含评审意见。

---

### Stage 5:跨章交接(Review)

**执行者**:Review Agent

**为什么必须自动做**:`cross-chapter-state.md` 是章节间传递术语与结论的**唯一载体**。
下一章靠它拿到"上一章把这个符号定成了什么""哪些结论已经建立"。

**输入**:本章 `final.md` + `todo.md` + 现有的 `cross-chapter-state.md`

**输出**:更新 `cross-chapter-state.md`。Review 先写候选文件,Python 校验通过后才原子
替换目标——这种失败(比如它把前几章的条目删了)只能在写完之后发现,那时如果目标文件
已经被覆盖,前章的术语约定就找不回来了。

**工作流**:按三个小节 upsert 本章的条目。每条 bullet 必须以 `- [<章节文件夹名>] ` 开头,
并替换同前缀的旧条目——所以重跑是幂等的:

| 小节 | 写什么 |
|---|---|
| `## Terminology Decisions` | 本章固定的术语拼写、缩写展开、符号定义(取自 plan 的术语表) |
| `## Per-Chapter Key Claims` | **一句话**说明本章确立了什么,供后续章节引用而不必重读全文 |
| `## Unresolved Cross-Chapter Issues` | 只写**后续章节**必须解决或协调的项,本章局部问题不写 |

**校验失败** → 本章判为未完成,`cross-chapter-state.md` 保持原样,`--all` 因此停下——
而不是让后面每一章在缺失术语约定的前提下各写一套。

---

## 编译与门禁(latex/build.py,不经 Agent)

```
bibliography.md ─convert_bib→ references.bib
章节 final.md   ─convert→ sections/*.tex
                        │
                        ▼
   引用闭合门禁(前) → pdflatex/bibtex → 带日志再解析(后)
                        │
                        ▼
                  main.pdf
```

`build.py` 在编译前查引用闭合(`\cite` key 是否都在 bib),编译后扫 log 的 undefined,
有问题非零退出。

---

## 防幻觉规则(核心)

1. **贡献的唯一来源是 `idea.md`**。框架**绝不替你编造贡献或机制**。设计细节缺失标
   `[DESIGN DETAIL NEEDED]`;`idea.md` 缺失**或还是未填的模板**→ idea 类章节直接中止。
2. **数字的唯一来源是 `data/` 结果库**。正文任何数字必须能从 context-pack 追溯到结果文件。
3. **结构的唯一来源是 `outline.md`**。Agent 不自行增删章节,也不自行改小节字数。`--expand`
   也不例外:它只能往章节里补小节,改动章数/标题/`type:` 会被拒绝采纳。
4. **缺数据标 `[MISSING DATA]` / `[UNKNOWN]`,绝不猜**。不臆想硬件、超参、数据集大小。
5. **门禁 fail-closed 但分级**:data 类章节结果库为空 → 阻止起草;idea 类章节结果库为空 →
   继续但标 UNVERIFIED(方法章可以先于实验写);缺 `idea.md` → idea 类章节阻止起草。
6. **Results 与 Discussion 必须引同一组数**。
7. **证据挖掘答不出就写 "I cannot answer from the provided evidence"**,不留空档硬编。
8. **引用补全只用你提供的 bibliography**:review 标记缺引用句子 → Python 从本地 bib 匹配
   key 插 `\cite`;匹配不到标 needs_human 交人工,**绝不编造引用**。
   `search_literature` 的网页命中标为"线索,不可引"——它们没有本地 bib 条目,
   插 `\cite` 会造成悬空引用。
9. **不许跨类型代偿**:方法章不许用实验结果代替机制解释,结果章不许用方法阐述填充篇幅。
